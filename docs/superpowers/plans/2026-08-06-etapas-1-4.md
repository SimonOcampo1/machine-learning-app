# Plan de implementación — Etapas 1 a 4

> **Para agentes ejecutores:** SUB-SKILL REQUERIDA: usar `superpowers:subagent-driven-development` (recomendado) o `superpowers:executing-plans` para implementar este plan tarea por tarea. Los pasos usan checkbox (`- [ ]`) para seguimiento.

**Goal:** Dejar desplegado en Vercel un sitio de estudio de ML con el design system completo, el tema de regresión lineal construido de punta a punta con los cinco tipos de ejercicio, y el progreso persistiendo por usuario entre dispositivos.

**Architecture:** Sitio estático sin build step: HTML por página, tres archivos JS globales (`progreso.js`, `ejercicios.js`, `anim.js`) y un `shared.css` con tokens en oklch. `data/temario.json` es la fuente de verdad de la estructura y desde ahí se renderizan el roadmap y los filtros. Una única serverless function en `api/progress.js` verifica el ID token de Google y lee/escribe un JSON por usuario en Redis.

**Tech Stack:** HTML5, CSS con custom properties y oklch, JavaScript sin transpilar. GSAP 3 con ScrollTrigger y DrawSVGPlugin por CDN. Pyodide por CDN, en demanda. Google Identity Services. Vercel Functions con runtime Node 20. Upstash Redis por su API REST. Tests con `node --test` de la stdlib.

**Spec:** `docs/superpowers/specs/2026-08-06-machine-learning-app-design.md`

## Global Constraints

- **Cero dependencias de runtime.** No se crea `node_modules`, no se instala nada con npm. Todo lo del navegador entra por CDN; todo lo del servidor sale de la stdlib de Node.
- **Sin build step.** Ningún archivo se compila, transpila ni empaqueta. Lo que está en el repo es lo que sirve Vercel.
- **Node 20 o superior** en las funciones (`fetch` y `crypto.createPublicKey` con formato `jwk` son globales/estables desde ahí).
- **Contenido en español, nombres de clases e identificadores en inglés o español consistente por archivo.** El contenido visible para David y Abel siempre en español rioplatense.
- **Ningún color hexadecimal literal dentro de un `<svg>`.** Siempre `var(--accent)`, `var(--ink)`, `var(--rule)`, `var(--accent-tint)`.
- **Toda animación respeta `prefers-reduced-motion`.** Con la preferencia activa el diagrama muestra su estado final completo, nunca una pantalla vacía.
- **El acento de fase nunca se usa en texto de lectura.** Cuerpo siempre `var(--ink)` sobre `var(--bg)`.
- **Todo archivo JS compartido termina con** `if (typeof module !== "undefined") module.exports = X;` para poder testearlo con `node --test` sin cambiar cómo lo carga el navegador.
- **Ningún test se limita a comprobar que una función devuelve el tipo correcto.** Un test que pasaría igual si la función fuera `() => []` no es un test. Cada chequeo, corrector o rama de lógica lleva al menos un caso con datos rotos a propósito, que verifica que el error aparece **y** que su mensaje nombra el problema, más un caso válido que verifica que no aparece. Para que eso sea posible, la lógica se extrae en funciones puras que reciben sus datos por argumento en vez de leer del disco o del DOM.
- **Ninguna página enlaza a un archivo que todavía no existe.** La navegación de estas cuatro etapas tiene Inicio y Temas nada más; Estudio y Práctica se suman en la Etapa 6. Los temas sin escribir se muestran atenuados y sin `href`.
- **Commits en español, sin trailers de Claude/Anthropic.**

---

## Estructura de archivos

| Archivo | Responsabilidad |
|---|---|
| `package.json` | Solo declara nombre, `type: commonjs` y el piso de Node. Sin dependencias. |
| `data/temario.json` | Fuente de verdad: 6 fases, 24 temas, slug, título, hue, archivo. |
| `shared.css` | Tokens (§Tarea 3) y componentes (§Tarea 4). Único stylesheet. |
| `shared.js` | Tema claro/oscuro, reveal on scroll, barra de progreso de lectura, año del pie. |
| `anim.js` | Helpers sobre GSAP: trazado de diagramas, secuencias fijadas, control de reducción de movimiento. |
| `progreso.js` | Estado del usuario: lectura/escritura local, fusión, cálculo de estados y porcentaje, sync. |
| `ejercicios.js` | Motor de los cinco tipos de ejercicio y sus correctores. |
| `api/progress.js` | Verificación de JWT, allowlist, lectura y escritura en Redis. |
| `verificar.js` | Chequeos de integridad del sitio. Se corre a mano y en el pre-deploy. |
| `progreso.test.js` | Pruebas de la fusión y del cálculo de estado. |
| `ejercicios.test.js` | Pruebas de los correctores. |
| `api/progress.test.js` | Pruebas de la verificación de token. |
| `index.html` | Hero y roadmap. |
| `temas.html` | Índice completo. |
| `muestra.html` | Kitchen sink del design system. No se enlaza desde la nav. |
| `concept-09-regresion-lineal.html` | El slice vertical. |

---

# ETAPA 1 — Validación del temario

### Tarea 1: Extraer los índices y validar el temario

**Files:**
- Create: `docs/fuentes/indices.md`
- Create: `docs/temario-validado.md`

**Interfaces:**
- Consumes: nada.
- Produces: `docs/temario-validado.md`, con la tabla definitiva de 24 temas: número, título, slug, fase, y capítulos exactos de cada libro. La Tarea 2 transcribe esa tabla a JSON.

- [ ] **Paso 1: Extraer el índice de cada libro**

Usar la skill `convert-documents-to-markdown` sobre los cinco PDFs de `assets/`, pidiendo **solo las páginas del índice** (típicamente las primeras 15 a 25 de cada uno, en numeración romana). No convertir los libros completos.

Los cinco archivos están en `assets/`:
- `[Information Science and Statistics ] Christopher M. Bishop - Pattern Recognition and Machine Learning (2006, Springer) - libgen.li.pdf`
- `[Springer Series in Statistics] Trevor Hastie, Robert Tibshirani, Jerome Friedman - The Elements of Statistical Learning_ Data Mining, Inference, and Prediction,.pdf`
- `An Introduction To Statistical Learning With Applications In R (Gareth James, Daniela Witten, Trevor Hastie etc.) (z-library.sk, 1lib.sk, z-lib.sk).pdf`
- `Hands-On Machine Learning with Scikit-Learn, Keras, and TensorFlow (Aurélien Géron) (z-library.sk, 1lib.sk, z-lib.sk).pdf`
- `Ian Goodfellow, Yoshua Bengio, Aaron Courville, Francis Bach - Deep Learning (Adaptive Computation and Machine Learning Series) (2017, MIT Press) - libgen.li.pdf`

Guardar los cinco índices en `docs/fuentes/indices.md`, con un encabezado `## <Sigla>` por libro. Siglas: `ISLR`, `ESL`, `Bishop`, `Géron`, `Goodfellow`.

- [ ] **Paso 2: Contrastar contra el temario del spec**

Leer §3 del spec. Para cada uno de los 24 temas, buscar en `docs/fuentes/indices.md` el capítulo que el spec le asigna y confirmar que **existe y trata de eso**. Anotar cada discrepancia.

Buscar además tres cosas concretas:
1. Temas que los cinco libros tratan como fundamentales y no están en la lista.
2. Temas de la lista que ningún libro cubre bien (candidatos a apoyarse en fuente web).
3. Capítulos mal numerados en el spec — las ediciones difieren, sobre todo ISLR (1ª vs 2ª edición) y Géron (2ª vs 3ª).

- [ ] **Paso 3: Escribir `docs/temario-validado.md`**

Una tabla con estas columnas exactas: `n` (01–24), `titulo`, `slug`, `fase` (0–5), `fuente_principal`, `fuentes_apoyo`, `nota`. La columna `nota` explica cualquier desvío respecto del spec.

Regla al resolver discrepancias: la cantidad de temas (24) y las 6 fases se mantienen fijas. Si un tema hay que cambiarlo, se cambia por otro dentro de la misma fase.

- [ ] **Paso 4: Commit**

```bash
git add docs/fuentes/indices.md docs/temario-validado.md
git commit -m "docs: índices de los 5 libros y temario validado contra el canon"
```

Nota: `docs/fuentes/` está en `.gitignore`. Forzar la inclusión de este archivo con `git add -f docs/fuentes/indices.md`, porque los índices sí son referencia estable y liviana; las extracciones de capítulos posteriores quedan ignoradas.

---

### Tarea 2: `data/temario.json` y el verificador de estructura

**Files:**
- Create: `package.json`
- Create: `data/temario.json`
- Create: `verificar.js`

**Interfaces:**
- Consumes: la tabla de `docs/temario-validado.md`.
- Produces: `data/temario.json` con la forma `{ fases: Fase[] }`, donde
  `Fase = { n: number, nombre: string, hue: number, temas: Tema[] }` y
  `Tema = { n: number, titulo: string, slug: string, archivo: string }`.
  Todas las tareas siguientes leen esta estructura.
  `verificar.js` expone `module.exports = { verificar }` y `verificar()` devuelve `string[]` de errores (vacío si está todo bien).

- [ ] **Paso 1: Crear `package.json`**

```json
{
  "name": "ml-estudio",
  "private": true,
  "type": "commonjs",
  "engines": { "node": ">=20" },
  "scripts": {
    "verificar": "node verificar.js",
    "test": "node --test"
  }
}
```

Sin campo `dependencies`. Si en algún momento aparece, es un error del plan.

- [ ] **Paso 2: Escribir `data/temario.json`**

Transcribir la tabla de `docs/temario-validado.md`. Los hues son los del spec §4 y no se cambian. Estructura completa, las 6 fases y los 24 temas:

```json
{
  "fases": [
    { "n": 0, "nombre": "Piso", "hint": "Nadie toca un modelo antes de saber qué es un vector", "hue": 220,
      "temas": [
        { "n": 1, "titulo": "Python esencial", "slug": "python", "archivo": "concept-01-python.html" },
        { "n": 2, "titulo": "NumPy y pensar en vectores", "slug": "numpy", "archivo": "concept-02-numpy.html" },
        { "n": 3, "titulo": "Álgebra lineal mínima", "slug": "algebra", "archivo": "concept-03-algebra.html" },
        { "n": 4, "titulo": "Probabilidad y estadística mínima", "slug": "probabilidad", "archivo": "concept-04-probabilidad.html" }
      ] },
    { "n": 1, "nombre": "Aprender de los datos", "hint": "Qué significa que una máquina aprenda", "hue": 285,
      "temas": [
        { "n": 5, "titulo": "Qué es machine learning", "slug": "que-es-ml", "archivo": "concept-05-que-es-ml.html" },
        { "n": 6, "titulo": "El flujo de trabajo", "slug": "flujo", "archivo": "concept-06-flujo.html" },
        { "n": 7, "titulo": "Train/test y validación cruzada", "slug": "validacion", "archivo": "concept-07-validacion.html" },
        { "n": 8, "titulo": "Sesgo, varianza y overfitting", "slug": "sesgo-varianza", "archivo": "concept-08-sesgo-varianza.html" }
      ] },
    { "n": 2, "nombre": "Regresión", "hint": "Predecir un número", "hue": 145,
      "temas": [
        { "n": 9, "titulo": "Regresión lineal simple", "slug": "regresion-lineal", "archivo": "concept-09-regresion-lineal.html" },
        { "n": 10, "titulo": "Regresión múltiple y descenso de gradiente", "slug": "gradiente", "archivo": "concept-10-gradiente.html" },
        { "n": 11, "titulo": "Regularización: Ridge y Lasso", "slug": "regularizacion", "archivo": "concept-11-regularizacion.html" }
      ] },
    { "n": 3, "nombre": "Clasificación", "hint": "Predecir una categoría", "hue": 55,
      "temas": [
        { "n": 12, "titulo": "Clasificación y regresión logística", "slug": "logistica", "archivo": "concept-12-logistica.html" },
        { "n": 13, "titulo": "Métricas de clasificación", "slug": "metricas", "archivo": "concept-13-metricas.html" },
        { "n": 14, "titulo": "K vecinos más cercanos", "slug": "knn", "archivo": "concept-14-knn.html" },
        { "n": 15, "titulo": "Naïve Bayes", "slug": "naive-bayes", "archivo": "concept-15-naive-bayes.html" },
        { "n": 16, "titulo": "SVM y el truco del kernel", "slug": "svm", "archivo": "concept-16-svm.html" }
      ] },
    { "n": 4, "nombre": "Árboles y no supervisado", "hint": "Decidir por ramas, y aprender sin etiquetas", "hue": 350,
      "temas": [
        { "n": 17, "titulo": "Árboles de decisión", "slug": "arboles", "archivo": "concept-17-arboles.html" },
        { "n": 18, "titulo": "Random Forest y bagging", "slug": "random-forest", "archivo": "concept-18-random-forest.html" },
        { "n": 19, "titulo": "Boosting", "slug": "boosting", "archivo": "concept-19-boosting.html" },
        { "n": 20, "titulo": "Clustering: K-means y jerárquico", "slug": "clustering", "archivo": "concept-20-clustering.html" },
        { "n": 21, "titulo": "Reducción de dimensionalidad: PCA", "slug": "pca", "archivo": "concept-21-pca.html" }
      ] },
    { "n": 5, "nombre": "Redes neuronales", "hint": "De una neurona a una red que aprende sola", "hue": 330,
      "temas": [
        { "n": 22, "titulo": "Del perceptrón a la red multicapa", "slug": "perceptron", "archivo": "concept-22-perceptron.html" },
        { "n": 23, "titulo": "Backpropagation", "slug": "backprop", "archivo": "concept-23-backprop.html" },
        { "n": 24, "titulo": "CNNs y qué sigue", "slug": "cnn", "archivo": "concept-24-cnn.html" }
      ] }
  ]
}
```

Ajustar títulos y slugs si la Tarea 1 encontró discrepancias. Los `hue` no se tocan.

- [ ] **Paso 3: Escribir el test de estructura**

Crear `verificar.test.js`:

```js
const { test } = require("node:test");
const assert = require("node:assert");
const { verificar } = require("./verificar.js");

test("el temario tiene 6 fases y 24 temas con slugs únicos", () => {
  const t = require("./data/temario.json");
  assert.strictEqual(t.fases.length, 6);
  const temas = t.fases.flatMap(f => f.temas);
  assert.strictEqual(temas.length, 24);
  assert.strictEqual(new Set(temas.map(x => x.slug)).size, 24);
  assert.deepStrictEqual(temas.map(x => x.n), Array.from({ length: 24 }, (_, i) => i + 1));
});

test("verificar() devuelve un array", () => {
  assert.ok(Array.isArray(verificar()));
});
```

- [ ] **Paso 4: Correr el test y verificar que falla**

Run: `node --test verificar.test.js`
Expected: FAIL — `Cannot find module './verificar.js'`

- [ ] **Paso 5: Escribir `verificar.js`**

Por ahora solo los chequeos que no dependen de archivos HTML todavía inexistentes. La Tarea 19 lo completa.

```js
const fs = require("node:fs");
const path = require("node:path");

const RAIZ = __dirname;

function verificar() {
  const errores = [];
  const temario = JSON.parse(fs.readFileSync(path.join(RAIZ, "data/temario.json"), "utf8"));
  const temas = temario.fases.flatMap(f => f.temas);

  // Slugs y números únicos y correlativos
  const slugs = new Set();
  for (const t of temas) {
    if (slugs.has(t.slug)) errores.push(`slug duplicado: ${t.slug}`);
    slugs.add(t.slug);
    if (!/^concept-\d{2}-[a-z0-9-]+\.html$/.test(t.archivo)) {
      errores.push(`nombre de archivo inválido: ${t.archivo}`);
    }
  }

  // Cada tema declarado debe tener su HTML, si ya fue escrito
  for (const t of temas) {
    const p = path.join(RAIZ, t.archivo);
    if (fs.existsSync(p)) {
      const html = fs.readFileSync(p, "utf8");
      // Ningún hexadecimal literal dentro de un <svg>
      for (const svg of html.match(/<svg[\s\S]*?<\/svg>/g) || []) {
        const hex = svg.match(/#[0-9a-fA-F]{3,8}\b/g);
        if (hex) errores.push(`${t.archivo}: color literal en <svg>: ${hex.join(", ")}`);
      }
    }
  }

  // Archivos HTML huérfanos: existen pero no están en el temario
  const declarados = new Set(temas.map(t => t.archivo));
  for (const f of fs.readdirSync(RAIZ)) {
    if (f.startsWith("concept-") && f.endsWith(".html") && !declarados.has(f)) {
      errores.push(`archivo huérfano, no está en temario.json: ${f}`);
    }
  }

  return errores;
}

if (require.main === module) {
  const errores = verificar();
  if (errores.length) {
    console.error(`✗ ${errores.length} problema(s):`);
    for (const e of errores) console.error("  - " + e);
    process.exit(1);
  }
  console.log("✓ verificación OK");
}

module.exports = { verificar };
```

- [ ] **Paso 6: Correr el test y verificar que pasa**

Run: `node --test verificar.test.js`
Expected: PASS, 2 tests.

Run también: `node verificar.js`
Expected: `✓ verificación OK`

- [ ] **Paso 7: Commit**

```bash
git add package.json data/temario.json verificar.js verificar.test.js
git commit -m "feat: temario.json como fuente de verdad y verificador de estructura"
```

---

# ETAPA 2 — Design system

### Tarea 3: Tokens de `shared.css`

**Files:**
- Create: `shared.css`

**Interfaces:**
- Consumes: los `hue` de `data/temario.json`.
- Produces: las custom properties que consumen todas las páginas y `anim.js`:
  `--bg --panel --ink --ink-mute --rule --accent --accent-tint --fase-h`
  y las clases de fase `.fase-0` … `.fase-5`.
  El tema vive en `document.documentElement.dataset.tema`, con valores `"oscuro"` o `"claro"`.

- [ ] **Paso 1: Escribir el bloque de tokens**

Este es el bloque completo. Va al principio de `shared.css`, antes de cualquier otra regla.

```css
/* ── Fuentes ─────────────────────────────────────────────── */
@import url("https://api.fontshare.com/v2/css?f[]=satoshi@400,500,700,900&display=swap");
/* Space Grotesk se importa acá aposta: es el fallback declarado en --font-display
   y sin importarlo el fallback no existiría. Va en el mismo pedido que la mono. */
@import url("https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Space+Grotesk:wght@400;500;700&display=swap");

/* ── Tokens base ─────────────────────────────────────────── */
:root {
  /* Crudos, tomados de gsap.com. No se usan directo salvo acá. */
  --just-black: #0e100f;
  --off-black: #191919;
  --surface-white: #fffce1;
  --s75: #bbbaa6;
  --s50: #7c7c6f;
  --s25: #42433d;

  /* Hue de fase por defecto: verde firma de GSAP */
  --fase-h: 145;

  --font-display: "Satoshi", "Space Grotesk", system-ui, sans-serif;
  --font-mono: "JetBrains Mono", ui-monospace, "SFMono-Regular", monospace;

  /* Escala tipográfica fluida */
  --t-display: clamp(2.8rem, 1.5rem + 6.5vw, 7rem);
  --t-h1: clamp(2.2rem, 1.4rem + 3.6vw, 4.2rem);
  --t-h2: clamp(1.6rem, 1.2rem + 1.8vw, 2.6rem);
  --t-h3: clamp(1.15rem, 1rem + 0.7vw, 1.5rem);
  --t-body: clamp(1rem, 0.96rem + 0.2vw, 1.125rem);
  --t-small: 0.875rem;

  --medida: 68ch;          /* medida máxima de lectura */
  --radio: 14px;
  --radio-lg: 24px;

  /* Espaciado en escala de 4 */
  --e1: 0.25rem;  --e2: 0.5rem;   --e3: 0.75rem;  --e4: 1rem;
  --e6: 1.5rem;   --e8: 2rem;     --e12: 3rem;    --e16: 4rem;
  --e24: 6rem;    --e32: 8rem;

  --transicion: 180ms cubic-bezier(0.4, 0, 0.2, 1);
}

/* ── Tema oscuro (por defecto) ───────────────────────────── */
:root, [data-tema="oscuro"] {
  --bg: var(--just-black);
  --panel: var(--off-black);
  --ink: var(--surface-white);
  --ink-mute: var(--s75);
  --rule: var(--s25);
  --accent: oklch(72% 0.19 var(--fase-h));
  --accent-tint: oklch(32% 0.08 var(--fase-h));
  --sombra: 0 1px 0 var(--rule);
}

/* ── Tema claro ──────────────────────────────────────────── */
[data-tema="claro"] {
  --bg: var(--surface-white);
  --panel: #fffef4;
  --ink: var(--just-black);
  --ink-mute: var(--s50);
  --rule: #ddd9bd;
  --accent: oklch(48% 0.16 var(--fase-h));
  --accent-tint: oklch(94% 0.05 var(--fase-h));
  --sombra: 0 1px 0 var(--rule);
}

/* ── Hue por fase: el único knob que cambia ──────────────── */
.fase-0 { --fase-h: 220; }   /* azul      */
.fase-1 { --fase-h: 285; }   /* lila      */
.fase-2 { --fase-h: 145; }   /* verde     */
.fase-3 { --fase-h: 55;  }   /* tangerina */
.fase-4 { --fase-h: 350; }   /* rosa      */
.fase-5 { --fase-h: 330; }   /* magenta   */

/* ── Base ────────────────────────────────────────────────── */
*, *::before, *::after { box-sizing: border-box; }

html {
  scroll-behavior: smooth;
  -webkit-text-size-adjust: 100%;
}

body {
  margin: 0;
  background: var(--bg);
  color: var(--ink);
  font-family: var(--font-display);
  font-size: var(--t-body);
  font-weight: 400;
  line-height: 1.65;
  -webkit-font-smoothing: antialiased;
  transition: background var(--transicion), color var(--transicion);
}

h1, h2, h3 {
  font-weight: 900;
  line-height: 0.96;
  letter-spacing: -0.035em;
  margin: 0 0 var(--e4);
  text-wrap: balance;
}
h1 { font-size: var(--t-h1); }
h2 { font-size: var(--t-h2); }
h3 { font-size: var(--t-h3); letter-spacing: -0.02em; line-height: 1.15; }

p { margin: 0 0 var(--e4); max-width: var(--medida); text-wrap: pretty; }

a { color: var(--accent); text-decoration-thickness: 1px; text-underline-offset: 3px; }

code, pre, .mono { font-family: var(--font-mono); }

:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 3px;
  border-radius: 3px;
}

@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

- [ ] **Paso 2: Verificar que `oklch()` acepta una variable como hue**

Crear un archivo temporal `t.html` con `<div style="--h:145;background:oklch(72% .19 var(--h));width:80px;height:80px"></div>`, abrirlo en el navegador y confirmar que se ve verde brillante, no negro ni transparente.

Si el navegador no lo soporta, el fallback es declarar los seis acentos como hex explícitos por clase de fase. Anotarlo y seguir; no bloquear.

Borrar `t.html` después.

- [ ] **Paso 3: Commit**

```bash
git add shared.css
git commit -m "feat: tokens del design system en oklch con hue por fase"
```

---

### Tarea 4: Componentes de `shared.css`

**Files:**
- Modify: `shared.css` (agregar al final)

**Interfaces:**
- Consumes: los tokens de la Tarea 3.
- Produces: las clases que usan todas las páginas:
  `.nav .nav-brand .nav-link .nav-link.on .theme-tog`,
  `.envoltura .hero .hero-c .eyebrow .regla`,
  `.section .sec-no .sec-body`,
  `.callout .callout.formula .callout.warn .callout.ok`,
  `.diagram`, y las de SVG `.dnode-bg .dedge .dtxt .dlabel`,
  `.rm-phase .rm-phase-meta .rm-cards .rm-card` con estados `.on-progreso` y `.completa`,
  `.concept-nav`, `.tarjeta`, `.barra-progreso`.

- [ ] **Paso 1: Escribir el bloque de layout y navegación**

```css
/* ── Layout ──────────────────────────────────────────────── */
.envoltura { width: min(1180px, 100% - 2.5rem); margin-inline: auto; }
.estrecho  { width: min(760px, 100% - 2.5rem); margin-inline: auto; }

/* ── Navegación ──────────────────────────────────────────── */
nav {
  position: sticky; top: 0; z-index: 50;
  display: flex; align-items: center; justify-content: space-between;
  gap: var(--e6);
  padding: var(--e4) max(1.25rem, calc((100% - 1180px) / 2));
  background: color-mix(in oklab, var(--bg) 88%, transparent);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--rule);
}
.nav-brand {
  font-weight: 900; letter-spacing: -0.03em; font-size: var(--t-small);
  text-transform: uppercase; color: var(--ink); text-decoration: none;
}
.nav-right { display: flex; align-items: center; gap: var(--e6); }
.nav-link {
  font-size: var(--t-small); font-weight: 500; color: var(--ink-mute);
  text-decoration: none; transition: color var(--transicion);
}
.nav-link:hover { color: var(--ink); }
.nav-link.on { color: var(--accent); font-weight: 700; }
.theme-tog {
  background: none; border: 1px solid var(--rule); color: var(--ink);
  border-radius: 999px; width: 34px; height: 34px; cursor: pointer;
  font-size: 15px; line-height: 1; transition: border-color var(--transicion);
}
.theme-tog:hover { border-color: var(--accent); }

/* Barra de progreso de lectura */
.barra-progreso {
  position: fixed; top: 0; left: 0; height: 2px; width: 0%;
  background: var(--accent); z-index: 60; transition: width 80ms linear;
}
```

- [ ] **Paso 2: Escribir el bloque editorial (hero, secciones, callouts)**

```css
/* ── Hero ────────────────────────────────────────────────── */
.hero, .hero-c { padding-block: var(--e24) var(--e16); }
.eyebrow {
  font-family: var(--font-mono); font-size: var(--t-small);
  text-transform: uppercase; letter-spacing: 0.14em;
  color: var(--accent); margin-bottom: var(--e4);
}
.hero .w1, .hero .w2 { display: block; font-size: var(--t-display); line-height: 0.9; }
.hero .w1 { font-weight: 900; letter-spacing: -0.045em; }
.hero .w2 { font-weight: 400; letter-spacing: -0.03em; color: var(--ink-mute); }
.lede { font-size: clamp(1.1rem, 1rem + 0.6vw, 1.4rem); color: var(--ink-mute); max-width: 58ch; }
.regla { height: 1px; background: var(--rule); border: 0; margin-block: var(--e8); }

.breadcrumb {
  font-family: var(--font-mono); font-size: var(--t-small);
  color: var(--ink-mute); margin-bottom: var(--e6);
}
.breadcrumb a { color: var(--ink-mute); }

/* ── Secciones numeradas ─────────────────────────────────── */
.section {
  display: grid; grid-template-columns: 4.5rem 1fr;
  gap: var(--e8); padding-block: var(--e16);
  border-top: 1px solid var(--rule);
}
.sec-no {
  font-family: var(--font-mono); font-size: var(--t-small); font-weight: 700;
  color: var(--accent); position: sticky; top: 5.5rem; align-self: start;
}
.sec-body > :last-child { margin-bottom: 0; }
@media (max-width: 720px) {
  .section { grid-template-columns: 1fr; gap: var(--e4); }
  .sec-no { position: static; }
}

/* ── Callouts ────────────────────────────────────────────── */
.callout {
  border-left: 3px solid var(--accent);
  background: var(--accent-tint);
  padding: var(--e4) var(--e6);
  border-radius: 0 var(--radio) var(--radio) 0;
  margin-block: var(--e6);
}
.callout > :last-child { margin-bottom: 0; }
.callout.formula {
  font-family: var(--font-mono); font-size: 1.05rem;
  text-align: center; padding-block: var(--e6);
}
.callout.warn { --fase-h: 55; }
.callout.err  { --fase-h: 25; }
.callout.ok   { --fase-h: 145; }
.callout .etiqueta {
  display: block; font-family: var(--font-mono); font-size: 0.75rem;
  text-transform: uppercase; letter-spacing: 0.12em;
  color: var(--accent); margin-bottom: var(--e2);
}
```

Nota sobre `.callout.warn` y `.callout.err`: redefinen `--fase-h` localmente, así que su acento y su tinte se recalculan solos y siguen respetando el tema claro/oscuro. No hay ningún color hardcodeado.

- [ ] **Paso 3: Escribir el bloque de diagramas SVG**

```css
/* ── Diagramas ───────────────────────────────────────────── */
.diagram {
  margin-block: var(--e12);
  padding: var(--e8);
  background: var(--panel);
  border: 1px solid var(--rule);
  border-radius: var(--radio-lg);
}
.diagram svg { width: 100%; height: auto; display: block; overflow: visible; }
.diagram figcaption {
  font-size: var(--t-small); color: var(--ink-mute);
  margin-top: var(--e4); text-align: center;
}

/* Helpers para el SVG. Ninguno lleva color literal. */
.dnode-bg { fill: var(--accent-tint); stroke: var(--accent); stroke-width: 2; }
.dnode-solid { fill: var(--accent); }
.dedge { stroke: var(--accent); stroke-width: 2; fill: none; stroke-linecap: round; }
.dedge-mute { stroke: var(--rule); stroke-width: 1.5; fill: none; }
.daxis { stroke: var(--ink-mute); stroke-width: 1.5; fill: none; }
.dtxt { fill: var(--ink); font-family: var(--font-mono); font-size: 13px; }
.dtxt-mute { fill: var(--ink-mute); font-family: var(--font-mono); font-size: 11px; }
.dlabel {
  fill: var(--accent); font-family: var(--font-mono); font-size: 12px;
  font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em;
}
```

- [ ] **Paso 4: Escribir el bloque de roadmap y tarjetas**

```css
/* ── Roadmap ─────────────────────────────────────────────── */
.rm-phase {
  display: grid; grid-template-columns: 15rem 1fr; gap: var(--e8);
  padding-block: var(--e12); border-top: 1px solid var(--rule);
}
.rm-phase-meta { position: sticky; top: 6rem; align-self: start; }
.rm-phase-n {
  font-family: var(--font-mono); font-size: 3rem; font-weight: 700;
  color: var(--accent); line-height: 1;
}
.rm-phase-hint { font-size: var(--t-small); color: var(--ink-mute); max-width: 26ch; }
.rm-cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(230px, 1fr)); gap: var(--e4); }

.rm-card {
  display: block; padding: var(--e6); text-decoration: none; color: var(--ink);
  background: var(--panel); border: 1px solid var(--rule); border-radius: var(--radio);
  transition: transform var(--transicion), border-color var(--transicion);
}
.rm-card:hover { transform: translateY(-3px); border-color: var(--accent); }
.rm-card-n { font-family: var(--font-mono); font-size: var(--t-small); color: var(--ink-mute); }
.rm-card-t { font-weight: 700; letter-spacing: -0.02em; margin-top: var(--e2); }
.rm-card-estado {
  display: inline-block; margin-top: var(--e3);
  font-family: var(--font-mono); font-size: 0.7rem;
  text-transform: uppercase; letter-spacing: 0.1em; color: var(--ink-mute);
}
.rm-card.en-progreso { border-color: var(--accent); }
.rm-card.en-progreso .rm-card-estado { color: var(--accent); }
.rm-card.completa { background: var(--accent-tint); border-color: var(--accent); }
.rm-card.completa .rm-card-estado { color: var(--accent); font-weight: 700; }

@media (max-width: 860px) {
  .rm-phase { grid-template-columns: 1fr; }
  .rm-phase-meta { position: static; }
}

/* ── Navegación entre conceptos ──────────────────────────── */
.concept-nav {
  display: flex; justify-content: space-between; gap: var(--e6);
  padding-block: var(--e12); border-top: 1px solid var(--rule);
}
.concept-nav a { text-decoration: none; color: var(--ink); max-width: 45%; }
.concept-nav .cn-dir {
  display: block; font-family: var(--font-mono); font-size: var(--t-small);
  color: var(--ink-mute);
}
.concept-nav .cn-t { font-weight: 700; letter-spacing: -0.02em; }
.concept-nav .cn-next { text-align: right; margin-left: auto; }

/* ── Reveal on scroll (lo aplica shared.js) ──────────────── */
/* El estado oculto vive detrás de `.js`, que shared.js pone en <html> en su
   primera línea. Si el JS no carga, no corre, o está deshabilitado, la regla
   nunca aplica y el contenido queda visible. Ocultar por defecto y confiar en
   que el JS lo revele deja la página en blanco ante cualquier falla. */
.js .reveal { opacity: 0; transform: translateY(18px); transition: opacity 600ms ease, transform 600ms ease; }
.js .reveal.visible { opacity: 1; transform: none; }
@media (prefers-reduced-motion: reduce) {
  .js .reveal { opacity: 1; transform: none; }
}
```

- [ ] **Paso 5: Commit**

```bash
git add shared.css
git commit -m "feat: componentes del design system (nav, editorial, diagramas, roadmap)"
```

---

### Tarea 5: `shared.js`

**Files:**
- Create: `shared.js`

**Interfaces:**
- Consumes: las clases `.reveal`, `.barra-progreso`, `#theme-tog` de la Tarea 4.
- Produces: la global `toggleTheme()` que invoca el `onclick` de la nav. Lee y escribe `localStorage["ml-tema"]`. Aplica el tema en `document.documentElement.dataset.tema` **antes** del primer pintado.

- [ ] **Paso 1: Escribir `shared.js`**

```js
/* Marca que el JS corre. Va PRIMERO y sin esperar a nada: el CSS oculta
   los .reveal solo bajo `.js`, así que si este archivo no carga o revienta
   antes de esta línea, todo el contenido queda visible en vez de invisible. */
document.documentElement.classList.add("js");

/* ── Tema ────────────────────────────────────────────────── */
const CLAVE_TEMA = "ml-tema";

function temaInicial() {
  const guardado = localStorage.getItem(CLAVE_TEMA);
  if (guardado === "claro" || guardado === "oscuro") return guardado;
  return matchMedia("(prefers-color-scheme: light)").matches ? "claro" : "oscuro";
}

function aplicarTema(t) {
  document.documentElement.dataset.tema = t;
  const b = document.getElementById("theme-tog");
  if (b) {
    b.textContent = t === "oscuro" ? "☾" : "☀";
    b.setAttribute("aria-label", t === "oscuro" ? "Cambiar a tema claro" : "Cambiar a tema oscuro");
  }
}

function toggleTheme() {
  const t = document.documentElement.dataset.tema === "oscuro" ? "claro" : "oscuro";
  localStorage.setItem(CLAVE_TEMA, t);
  aplicarTema(t);
}

// Se corre inmediatamente, no en DOMContentLoaded: evita el flash de tema equivocado.
aplicarTema(temaInicial());

/* ── Reveal on scroll ────────────────────────────────────── */
/* Contrato con el CSS: `.js .reveal` es lo que oculta. La clase `.js` se puso
   arriba, antes del primer pintado, para que no haya flash de contenido que
   aparece y se esconde. La contracara es que si el observer no llega a
   montarse, ese contenido queda oculto para siempre — así que cualquier
   camino de falla saca la clase y devuelve todo a visible. */
function mostrarTodo() {
  document.documentElement.classList.remove("js");
}

function montarReveal() {
  const objetivos = document.querySelectorAll(".reveal");
  if (!objetivos.length) return;
  if (matchMedia("(prefers-reduced-motion: reduce)").matches) { mostrarTodo(); return; }
  if (typeof IntersectionObserver === "undefined") { mostrarTodo(); return; }

  const obs = new IntersectionObserver((entradas) => {
    for (const e of entradas) {
      if (e.isIntersecting) { e.target.classList.add("visible"); obs.unobserve(e.target); }
    }
  }, { rootMargin: "0px 0px -12% 0px" });
  objetivos.forEach(el => obs.observe(el));
}

/* ── Barra de progreso de lectura ────────────────────────── */
function montarBarra() {
  const barra = document.querySelector(".barra-progreso");
  if (!barra) return;
  const actualizar = () => {
    const alto = document.documentElement.scrollHeight - innerHeight;
    barra.style.width = alto > 0 ? `${Math.min(100, (scrollY / alto) * 100)}%` : "0%";
  };
  addEventListener("scroll", actualizar, { passive: true });
  addEventListener("resize", actualizar);
  actualizar();
}

/* ── Año del pie ─────────────────────────────────────────── */
function montarAnio() {
  document.querySelectorAll("[data-anio]").forEach(el => { el.textContent = new Date().getFullYear(); });
}

/* montarReveal va PRIMERO y aislado: es el único que puede dejar la página
   en blanco si falla. Si reventara un montador anterior, este nunca correría
   y el contenido quedaría oculto. Cada uno en su propio try/catch para que
   un error en la barra de progreso no se lleve puesto al resto. */
addEventListener("DOMContentLoaded", () => {
  try { montarReveal(); } catch (e) { mostrarTodo(); console.warn("reveal:", e); }
  for (const montar of [() => aplicarTema(document.documentElement.dataset.tema || temaInicial()), montarBarra, montarAnio]) {
    try { montar(); } catch (e) { console.warn("montaje:", e); }
  }
});
```

- [ ] **Paso 2: Commit**

```bash
git add shared.js
git commit -m "feat: tema claro/oscuro, reveal on scroll y barra de lectura"
```

---

### Tarea 6: `muestra.html` — kitchen sink y verificación de contraste

**Files:**
- Create: `muestra.html`

**Interfaces:**
- Consumes: todo `shared.css` y `shared.js`.
- Produces: nada que consuman otras tareas. Es la página de control visual del design system; no se enlaza desde la navegación.

- [ ] **Paso 1: Escribir la página**

Estructura mínima obligatoria, con las seis fases visibles a la vez para poder comparar:

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="robots" content="noindex">
  <title>Muestra del design system</title>
  <link rel="stylesheet" href="shared.css">
  <script src="shared.js"></script>
</head>
<body>
  <div class="barra-progreso"></div>
  <nav>
    <a href="index.html" class="nav-brand">ML · Muestra</a>
    <div class="nav-right">
      <button class="theme-tog" id="theme-tog" onclick="toggleTheme()" aria-label="Cambiar tema">☾</button>
    </div>
  </nav>

  <main class="envoltura">
    <header class="hero">
      <p class="eyebrow">Design system</p>
      <h1><span class="w1">Muestra</span><span class="w2">de componentes</span></h1>
      <p class="lede">Todos los tokens y componentes, en las seis fases, para revisar de un vistazo.</p>
    </header>

    <!-- Un bloque por fase -->
    <section class="section fase-0">
      <div class="sec-no">00</div>
      <div class="sec-body">
        <h2>Fase 0 · Piso</h2>
        <p>Párrafo de cuerpo para chequear medida de lectura, interlineado y color sobre el fondo.</p>
        <div class="callout"><span class="etiqueta">Nota</span>Callout normal.</div>
        <div class="callout formula">y = β₀ + β₁x + ε</div>
        <div class="callout warn"><span class="etiqueta">Cuidado</span>Callout de advertencia.</div>
        <div class="callout ok"><span class="etiqueta">Clave</span>Callout de confirmación.</div>
        <figure class="diagram">
          <svg viewBox="0 0 400 140" role="img" aria-label="Diagrama de muestra">
            <line class="daxis" x1="30" y1="110" x2="370" y2="110"/>
            <circle class="dnode-bg" cx="90" cy="60" r="26"/>
            <text class="dtxt" x="90" y="65" text-anchor="middle">x</text>
            <path class="dedge" d="M118 60 H 250"/>
            <rect class="dnode-bg" x="250" y="34" width="90" height="52" rx="10"/>
            <text class="dtxt" x="295" y="65" text-anchor="middle">f(x)</text>
            <text class="dlabel" x="184" y="48" text-anchor="middle">flujo</text>
          </svg>
          <figcaption>Cada color sale de una custom property.</figcaption>
        </figure>
      </div>
    </section>
    <!-- Repetir el bloque para fase-1 … fase-5, cambiando la clase y el número -->
  </main>
</body>
</html>
```

Repetir la `<section>` seis veces, una por clase `.fase-0` … `.fase-5`, cambiando también `.sec-no` y el `<h2>`.

- [ ] **Paso 2: Revisar en el navegador, los dos temas**

Abrir `muestra.html`. Alternar el tema con el botón. Confirmar en ambos:
- Los seis acentos se distinguen entre sí.
- El texto de los callouts se lee sin esfuerzo sobre `--accent-tint`.
- Los diagramas cambian de color con el tema sin tocar el markup.
- No hay flash de tema equivocado al recargar.

- [ ] **Paso 3: Medir contraste**

Con el inspector del navegador (pestaña Accesibilidad, o el cuentagotas del selector de color), medir y anotar en `docs/contraste.md`:

| Par | Requisito | Medido claro | Medido oscuro |
|---|---|---|---|
| `--ink` sobre `--bg` | ≥ 7:1 | | |
| `--ink-mute` sobre `--bg` | ≥ 4.5:1 | | |
| `--ink` sobre `--accent-tint` (las 6 fases) | ≥ 4.5:1 | | |
| `--accent` sobre `--bg`, uso en títulos grandes | ≥ 3:1 | | |

Cualquier par que no llegue se corrige subiendo o bajando la `L` del token en `shared.css`, no cambiando el hue. Repetir hasta que las cuatro filas pasen en ambos temas.

- [ ] **Paso 4: Invocar `impeccable`**

Con el kitchen sink funcionando y el contraste medido, invocar la skill `impeccable` sobre `muestra.html` para el refinamiento visual: ritmo tipográfico, densidad del espaciado, tratamiento de bordes y radios, y calidad general de la composición frente a la referencia de gsap.com.

Aplicar lo que salga de ahí a `shared.css`, no a `muestra.html`.

- [ ] **Paso 5: Commit**

```bash
git add muestra.html docs/contraste.md shared.css
git commit -m "feat: kitchen sink del design system con contraste verificado"
```

---

### Tarea 7: `anim.js` — helpers de animación

**Files:**
- Create: `anim.js`

**Interfaces:**
- Consumes: GSAP, ScrollTrigger y DrawSVGPlugin, cargados por CDN antes que este archivo.
- Produces: la global `Anim` con:
  - `Anim.reducido() → boolean`
  - `Anim.dibujar(selector, opts?) → gsap.core.Tween | null` — traza los `.dedge` del contenedor al entrar en viewport.
  - `Anim.aparecer(selector, opts?) → gsap.core.Tween | null` — escala los `.dnode-bg` desde 0.
  - `Anim.secuencia(selectorContenedor, pasos) → gsap.core.Timeline | null` — línea de tiempo fijada y atada al scroll, donde `pasos` es `Array<{ el: string, desde: object, hasta: object }>`.
  - `Anim.controles(selectorContenedor, timeline)` — inyecta un botón de reproducir/rebobinar.

- [ ] **Paso 1: Escribir `anim.js`**

```js
/* Requiere: gsap, ScrollTrigger, DrawSVGPlugin ya cargados. */
const Anim = (() => {
  const listo = typeof gsap !== "undefined";
  if (listo) gsap.registerPlugin(ScrollTrigger, DrawSVGPlugin);

  const reducido = () => matchMedia("(prefers-reduced-motion: reduce)").matches;

  /* Traza los caminos del diagrama al entrar en viewport.
     Con movimiento reducido, los deja dibujados al 100% de una. */
  function dibujar(sel, { duracion = 1.1, escalonado = 0.12 } = {}) {
    if (!listo) return null;
    const trazos = gsap.utils.toArray(`${sel} .dedge`);
    if (!trazos.length) return null;
    if (reducido()) { gsap.set(trazos, { drawSVG: "100%" }); return null; }
    gsap.set(trazos, { drawSVG: "0%" });
    return gsap.to(trazos, {
      drawSVG: "100%", duration: duracion, stagger: escalonado, ease: "power2.out",
      scrollTrigger: { trigger: sel, start: "top 78%", once: true }
    });
  }

  function aparecer(sel, { duracion = 0.5, escalonado = 0.08 } = {}) {
    if (!listo) return null;
    const nodos = gsap.utils.toArray(`${sel} .dnode-bg, ${sel} .dnode-solid`);
    if (!nodos.length) return null;
    if (reducido()) { gsap.set(nodos, { scale: 1, opacity: 1 }); return null; }
    return gsap.from(nodos, {
      scale: 0, opacity: 0, transformOrigin: "center", duration: duracion,
      stagger: escalonado, ease: "back.out(1.7)",
      scrollTrigger: { trigger: sel, start: "top 78%", once: true }
    });
  }

  /* Secuencia fijada: la sección se queda quieta mientras el scroll
     avanza el diagrama paso a paso. Es el recurso caro; usar poco. */
  function secuencia(sel, pasos) {
    if (!listo) return null;
    if (reducido() || innerWidth < 760) {
      // Estado final legible, sin pin. En móvil el pin es más molesto que útil.
      for (const p of pasos) gsap.set(p.el, p.hasta);
      return null;
    }
    const tl = gsap.timeline({
      scrollTrigger: {
        trigger: sel, start: "top top", end: `+=${pasos.length * 420}`,
        pin: true, scrub: 0.6, anticipatePin: 1
      }
    });
    for (const p of pasos) tl.fromTo(p.el, p.desde, { ...p.hasta, duration: 1 });
    return tl;
  }

  /* Botón de reproducir/rebobinar para diagramas que no van atados al scroll. */
  function controles(sel, tl) {
    const cont = document.querySelector(sel);
    if (!cont || !tl) return;
    const b = document.createElement("button");
    b.className = "diag-ctrl";
    b.type = "button";
    b.textContent = "↻ Repetir";
    b.addEventListener("click", () => { tl.restart(); });
    cont.appendChild(b);
  }

  return { reducido, dibujar, aparecer, secuencia, controles };
})();

if (typeof module !== "undefined") module.exports = Anim;
```

- [ ] **Paso 2: Agregar el estilo del botón de control a `shared.css`**

```css
.diag-ctrl {
  display: block; margin: var(--e4) auto 0;
  font-family: var(--font-mono); font-size: var(--t-small);
  background: none; color: var(--accent);
  border: 1px solid var(--rule); border-radius: 999px;
  padding: var(--e2) var(--e4); cursor: pointer;
  transition: border-color var(--transicion);
}
.diag-ctrl:hover { border-color: var(--accent); }
```

- [ ] **Paso 3: Probar en `muestra.html`**

Agregar antes de `</body>`:

```html
<script src="https://cdn.jsdelivr.net/npm/gsap@3.13.0/dist/gsap.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/gsap@3.13.0/dist/ScrollTrigger.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/gsap@3.13.0/dist/DrawSVGPlugin.min.js"></script>
<script src="anim.js"></script>
<script>
  Anim.dibujar(".fase-0 .diagram");
  Anim.aparecer(".fase-0 .diagram");
</script>
```

Confirmar en el navegador que la línea del diagrama se traza al hacer scroll. Después activar "reducir movimiento" en el sistema operativo, recargar, y confirmar que el diagrama aparece **completo y legible**, no vacío.

Verificar en la consola que las URLs del CDN respondan 200. Si `DrawSVGPlugin` no está en jsdelivr bajo esa ruta, buscar la ruta correcta en la documentación de GSAP y anotarla en el commit.

- [ ] **Paso 4: Commit**

```bash
git add anim.js shared.css muestra.html
git commit -m "feat: helpers de animación GSAP con degradación por movimiento reducido"
```

---

# ETAPA 3 — Slice vertical: regresión lineal

### Tarea 8: `progreso.js` — estado local y fusión

**Files:**
- Create: `progreso.js`
- Create: `progreso.test.js`

**Interfaces:**
- Consumes: `data/temario.json` (solo en `porcentaje`).
- Produces: la global `Progreso` con:
  - `Progreso.vacio() → Estado`
  - `Progreso.leer() → Estado` (de `localStorage["ml-progreso"]`)
  - `Progreso.guardar(estado) → void`
  - `Progreso.marcarLeido(slug) → Estado`
  - `Progreso.marcarQuiz(slug) → Estado`
  - `Progreso.registrar(slug, ejercicioId, puntaje) → Estado`
  - `Progreso.fusionar(a, b) → Estado` — pura, sin efectos.
  - `Progreso.estadoTema(estado, slug, totalEjercicios) → "sin-empezar" | "en-progreso" | "completa"`
  - `Progreso.porcentaje(estado, temario, totalesPorSlug) → number` (0–100, entero)

  Donde `Estado = { v: 1, actualizado: string, temas: Record<string, Tema> }`
  y `Tema = { leido: boolean, quiz: boolean, ejercicios: Record<string, number> }`.

- [ ] **Paso 1: Escribir los tests que fallan**

Crear `progreso.test.js`:

```js
const { test } = require("node:test");
const assert = require("node:assert");
const P = require("./progreso.js");

test("fusionar toma el máximo puntaje por ejercicio", () => {
  const a = { v: 1, temas: { rl: { leido: true, quiz: false, ejercicios: { e1: 100, e2: 40 } } } };
  const b = { v: 1, temas: { rl: { leido: false, quiz: true, ejercicios: { e2: 80, e3: 60 } } } };
  const r = P.fusionar(a, b);
  assert.deepStrictEqual(r.temas.rl.ejercicios, { e1: 100, e2: 80, e3: 60 });
  assert.strictEqual(r.temas.rl.leido, true);
  assert.strictEqual(r.temas.rl.quiz, true);
});

test("fusionar es conmutativa en los puntajes", () => {
  const a = { v: 1, temas: { rl: { leido: false, quiz: false, ejercicios: { e1: 30 } } } };
  const b = { v: 1, temas: { rl: { leido: false, quiz: false, ejercicios: { e1: 90 } } } };
  assert.deepStrictEqual(P.fusionar(a, b).temas.rl, P.fusionar(b, a).temas.rl);
});

test("fusionar tolera lados vacíos, nulos y sin temas", () => {
  const a = { v: 1, temas: { rl: { leido: true, quiz: true, ejercicios: { e1: 50 } } } };
  assert.deepStrictEqual(P.fusionar(a, null).temas.rl, a.temas.rl);
  assert.deepStrictEqual(P.fusionar(null, a).temas.rl, a.temas.rl);
  assert.deepStrictEqual(P.fusionar(null, null).temas, {});
  assert.deepStrictEqual(P.fusionar({}, {}).temas, {});
});

test("fusionar es idempotente", () => {
  const a = { v: 1, temas: { rl: { leido: true, quiz: true, ejercicios: { e1: 70 } } } };
  assert.deepStrictEqual(P.fusionar(a, a).temas, a.temas);
});

test("fusionar une temas que solo existen de un lado", () => {
  const a = { v: 1, temas: { rl: { leido: true, quiz: false, ejercicios: {} } } };
  const b = { v: 1, temas: { knn: { leido: false, quiz: true, ejercicios: { k1: 20 } } } };
  const r = P.fusionar(a, b);
  assert.deepStrictEqual(Object.keys(r.temas).sort(), ["knn", "rl"]);
});

test("estadoTema devuelve sin-empezar, en-progreso o completa", () => {
  const vacio = P.vacio();
  assert.strictEqual(P.estadoTema(vacio, "rl", 3), "sin-empezar");

  const parcial = { v: 1, temas: { rl: { leido: true, quiz: false, ejercicios: { e1: 100 } } } };
  assert.strictEqual(P.estadoTema(parcial, "rl", 3), "en-progreso");

  const full = { v: 1, temas: { rl: { leido: true, quiz: true, ejercicios: { e1: 100, e2: 60, e3: 80 } } } };
  assert.strictEqual(P.estadoTema(full, "rl", 3), "completa");
});

test("estadoTema no marca completa si falta el quiz", () => {
  const sinQuiz = { v: 1, temas: { rl: { leido: true, quiz: false, ejercicios: { e1: 100, e2: 60 } } } };
  assert.strictEqual(P.estadoTema(sinQuiz, "rl", 2), "en-progreso");
});

test("estadoTema ignora ejercicios con puntaje cero", () => {
  const cero = { v: 1, temas: { rl: { leido: true, quiz: true, ejercicios: { e1: 100, e2: 0 } } } };
  assert.strictEqual(P.estadoTema(cero, "rl", 2), "en-progreso");
});

test("porcentaje cuenta temas completos sobre el total del temario", () => {
  const temario = { fases: [{ n: 0, temas: [{ slug: "a" }, { slug: "b" }, { slug: "c" }, { slug: "d" }] }] };
  const totales = { a: 1, b: 1, c: 1, d: 1 };
  const est = {
    v: 1, temas: {
      a: { leido: true, quiz: true, ejercicios: { x: 100 } },
      b: { leido: true, quiz: false, ejercicios: { x: 100 } }
    }
  };
  assert.strictEqual(P.porcentaje(est, temario, totales), 25);
});
```

- [ ] **Paso 2: Correr y verificar que falla**

Run: `node --test progreso.test.js`
Expected: FAIL — `Cannot find module './progreso.js'`

- [ ] **Paso 3: Escribir `progreso.js`**

```js
const Progreso = (() => {
  const CLAVE = "ml-progreso";

  const vacio = () => ({ v: 1, actualizado: new Date().toISOString(), temas: {} });

  const temaVacio = () => ({ leido: false, quiz: false, ejercicios: {} });

  function leer() {
    try {
      const crudo = localStorage.getItem(CLAVE);
      if (!crudo) return vacio();
      const e = JSON.parse(crudo);
      if (!e || typeof e !== "object" || !e.temas) return vacio();
      return e;
    } catch {
      // JSON corrupto: mejor arrancar limpio que romper toda la página.
      return vacio();
    }
  }

  function guardar(estado) {
    estado.actualizado = new Date().toISOString();
    localStorage.setItem(CLAVE, JSON.stringify(estado));
    if (typeof Sync !== "undefined") Sync.encolar(estado);
    return estado;
  }

  function mutar(slug, fn) {
    const e = leer();
    e.temas[slug] = { ...temaVacio(), ...(e.temas[slug] || {}) };
    fn(e.temas[slug]);
    return guardar(e);
  }

  const marcarLeido = (slug) => mutar(slug, t => { t.leido = true; });
  const marcarQuiz  = (slug) => mutar(slug, t => { t.quiz = true; });
  const registrar   = (slug, id, puntaje) =>
    mutar(slug, t => { t.ejercicios[id] = Math.max(t.ejercicios[id] || 0, puntaje); });

  /* Fusión monótona: nada se pierde nunca. Conmutativa e idempotente. */
  function fusionar(a, b) {
    const ta = (a && a.temas) || {};
    const tb = (b && b.temas) || {};
    const salida = { v: 1, actualizado: new Date().toISOString(), temas: {} };

    for (const slug of new Set([...Object.keys(ta), ...Object.keys(tb)])) {
      const x = ta[slug] || {};
      const y = tb[slug] || {};
      const ejercicios = {};
      const ex = x.ejercicios || {};
      const ey = y.ejercicios || {};
      for (const id of new Set([...Object.keys(ex), ...Object.keys(ey)])) {
        ejercicios[id] = Math.max(ex[id] || 0, ey[id] || 0);
      }
      salida.temas[slug] = {
        leido: Boolean(x.leido || y.leido),
        quiz: Boolean(x.quiz || y.quiz),
        ejercicios
      };
    }
    return salida;
  }

  function estadoTema(estado, slug, totalEjercicios) {
    const t = (estado && estado.temas && estado.temas[slug]) || null;
    if (!t) return "sin-empezar";
    const resueltos = Object.values(t.ejercicios || {}).filter(p => p > 0).length;
    if (!t.leido && !t.quiz && resueltos === 0) return "sin-empezar";
    if (t.leido && t.quiz && resueltos >= totalEjercicios) return "completa";
    return "en-progreso";
  }

  function porcentaje(estado, temario, totalesPorSlug) {
    const temas = temario.fases.flatMap(f => f.temas);
    if (!temas.length) return 0;
    const completos = temas.filter(
      t => estadoTema(estado, t.slug, totalesPorSlug[t.slug] || 0) === "completa"
    ).length;
    return Math.round((completos / temas.length) * 100);
  }

  return { vacio, leer, guardar, marcarLeido, marcarQuiz, registrar, fusionar, estadoTema, porcentaje };
})();

if (typeof module !== "undefined") module.exports = Progreso;
```

- [ ] **Paso 4: Correr y verificar que pasa**

Run: `node --test progreso.test.js`
Expected: PASS, 9 tests.

Nota: los tests solo tocan funciones puras (`fusionar`, `estadoTema`, `porcentaje`, `vacio`), así que no necesitan `localStorage`. Si algún test lo tocara indirectamente, fallaría en Node — eso indicaría que la función no es pura y hay que separarla.

- [ ] **Paso 5: Commit**

```bash
git add progreso.js progreso.test.js
git commit -m "feat: estado de progreso local con fusión monótona"
```

---

### Tarea 9: `ejercicios.js` — correctores de MCQ y numérico

**Files:**
- Create: `ejercicios.js`
- Create: `ejercicios.test.js`
- Modify: `shared.css` (agregar bloque de ejercicios)

**Interfaces:**
- Consumes: `Progreso.registrar(slug, id, puntaje)` de la Tarea 8.
- Produces: la global `Ejercicios` con:
  - `Ejercicios.corregir(def, respuesta) → { ok: boolean, puntaje: number, msg: string }` — pura, sin DOM. `puntaje` es 0–100.
  - `Ejercicios.montar(contenedor, defs, slug) → void` — renderiza y cablea.

  Formas de `def`:
  ```js
  { tipo:"mcq", id:string, q:string, opts:string[], c:number, expl:string }
  { tipo:"num", id:string, q:string, resp:number, tol:number, unidad?:string,
    trampas?:Array<{val:number, msg:string}>, expl:string }
  ```

- [ ] **Paso 1: Escribir los tests que fallan**

Crear `ejercicios.test.js`:

```js
const { test } = require("node:test");
const assert = require("node:assert");
const E = require("./ejercicios.js");

const mcq = { tipo: "mcq", id: "m1", q: "¿?", opts: ["a", "b", "c", "d"], c: 2, expl: "porque sí" };

test("mcq correcto da 100", () => {
  const r = E.corregir(mcq, 2);
  assert.strictEqual(r.ok, true);
  assert.strictEqual(r.puntaje, 100);
});

test("mcq incorrecto da 0 y explica", () => {
  const r = E.corregir(mcq, 0);
  assert.strictEqual(r.ok, false);
  assert.strictEqual(r.puntaje, 0);
  assert.match(r.msg, /porque sí/);
});

const num = {
  tipo: "num", id: "n1", q: "¿?", resp: 2.35, tol: 0.01, expl: "se calcula así",
  trampas: [{ val: 3.35, msg: "Sumaste el intercepto de más." }]
};

test("numérico dentro de tolerancia da 100", () => {
  assert.strictEqual(E.corregir(num, 2.35).puntaje, 100);
  assert.strictEqual(E.corregir(num, 2.3549).puntaje, 100);
});

test("numérico fuera de tolerancia da 0", () => {
  assert.strictEqual(E.corregir(num, 2.5).puntaje, 0);
});

test("numérico diagnostica la trampa conocida", () => {
  const r = E.corregir(num, 3.35);
  assert.strictEqual(r.ok, false);
  assert.match(r.msg, /intercepto de más/);
});

test("numérico rechaza entrada no numérica sin romperse", () => {
  const r = E.corregir(num, NaN);
  assert.strictEqual(r.ok, false);
  assert.match(r.msg, /número/i);
});

test("la tolerancia es relativa para valores grandes", () => {
  const grande = { tipo: "num", id: "n2", q: "¿?", resp: 100000, tol: 0.01, expl: "" };
  assert.strictEqual(E.corregir(grande, 100500).puntaje, 100); // 0.5% < 1%
  assert.strictEqual(E.corregir(grande, 105000).puntaje, 0);   // 5%   > 1%
});

test("la tolerancia funciona cuando la respuesta es cero", () => {
  const cero = { tipo: "num", id: "n3", q: "¿?", resp: 0, tol: 0.01, expl: "" };
  assert.strictEqual(E.corregir(cero, 0).puntaje, 100);
  assert.strictEqual(E.corregir(cero, 0.005).puntaje, 100);
  assert.strictEqual(E.corregir(cero, 1).puntaje, 0);
});
```

- [ ] **Paso 2: Correr y verificar que falla**

Run: `node --test ejercicios.test.js`
Expected: FAIL — `Cannot find module './ejercicios.js'`

- [ ] **Paso 3: Escribir el núcleo de `ejercicios.js`**

```js
const Ejercicios = (() => {

  /* ── Correctores puros ────────────────────────────────── */

  function corregirMcq(def, elegida) {
    const ok = elegida === def.c;
    return {
      ok,
      puntaje: ok ? 100 : 0,
      msg: ok ? def.expl : `La correcta era «${def.opts[def.c]}». ${def.expl}`
    };
  }

  function corregirNum(def, valor) {
    if (typeof valor !== "number" || !Number.isFinite(valor)) {
      return { ok: false, puntaje: 0, msg: "Eso no es un número. Revisá lo que escribiste." };
    }
    // Tolerancia relativa, con piso absoluto para que funcione cerca de cero.
    const margen = Math.max(Math.abs(def.resp) * def.tol, def.tol);
    if (Math.abs(valor - def.resp) <= margen) {
      return { ok: true, puntaje: 100, msg: def.expl };
    }
    for (const t of def.trampas || []) {
      const margenT = Math.max(Math.abs(t.val) * def.tol, def.tol);
      if (Math.abs(valor - t.val) <= margenT) {
        return { ok: false, puntaje: 0, msg: t.msg };
      }
    }
    return { ok: false, puntaje: 0, msg: `No es. La respuesta es ${def.resp}. ${def.expl}` };
  }

  function corregir(def, respuesta) {
    switch (def.tipo) {
      case "mcq": return corregirMcq(def, respuesta);
      case "num": return corregirNum(def, respuesta);
      default: throw new Error(`tipo de ejercicio desconocido: ${def.tipo}`);
    }
  }

  return { corregir };
})();

if (typeof module !== "undefined") module.exports = Ejercicios;
```

- [ ] **Paso 4: Correr y verificar que pasa**

Run: `node --test ejercicios.test.js`
Expected: PASS, 8 tests.

- [ ] **Paso 5: Agregar el renderizado**

Ampliar el IIFE, antes del `return`, y cambiar el `return` a `{ corregir, montar }`:

```js
  /* ── Renderizado ──────────────────────────────────────── */

  function nodo(tag, clase, texto) {
    const n = document.createElement(tag);
    if (clase) n.className = clase;
    if (texto != null) n.textContent = texto;
    return n;
  }

  function pintarResultado(caja, res) {
    caja.className = `ej-fb ${res.ok ? "ok" : "no"}`;
    caja.textContent = res.msg;
    caja.hidden = false;
  }

  function montarMcq(def, slug) {
    const raiz = nodo("div", "ej ej-mcq");
    raiz.append(nodo("p", "ej-q", def.q));
    const lista = nodo("div", "ej-opts");
    const fb = nodo("div", "ej-fb");
    fb.hidden = true;
    let respondido = false;

    def.opts.forEach((texto, i) => {
      const b = nodo("button", "ej-opt", texto);
      b.type = "button";
      b.addEventListener("click", () => {
        if (respondido) return;
        respondido = true;
        const res = corregirMcq(def, i);
        b.classList.add(res.ok ? "ok" : "no");
        if (!res.ok) lista.children[def.c].classList.add("ok");
        lista.querySelectorAll(".ej-opt").forEach(x => { x.disabled = true; });
        pintarResultado(fb, res);
        Progreso.registrar(slug, def.id, res.puntaje);
      });
      lista.append(b);
    });

    raiz.append(lista, fb);
    return raiz;
  }

  function montarNum(def, slug) {
    const raiz = nodo("div", "ej ej-num");
    raiz.append(nodo("p", "ej-q", def.q));

    const fila = nodo("div", "ej-fila");
    const input = nodo("input", "ej-input");
    input.type = "number";
    input.step = "any";
    input.setAttribute("aria-label", "Tu respuesta");
    if (def.unidad) input.setAttribute("aria-describedby", `${def.id}-u`);

    const boton = nodo("button", "ej-enviar", "Comprobar");
    boton.type = "button";
    const fb = nodo("div", "ej-fb");
    fb.hidden = true;

    const enviar = () => {
      const res = corregirNum(def, input.valueAsNumber);
      pintarResultado(fb, res);
      Progreso.registrar(slug, def.id, res.puntaje);
    };
    boton.addEventListener("click", enviar);
    input.addEventListener("keydown", e => { if (e.key === "Enter") enviar(); });

    fila.append(input);
    if (def.unidad) {
      const u = nodo("span", "ej-unidad", def.unidad);
      u.id = `${def.id}-u`;
      fila.append(u);
    }
    fila.append(boton);
    raiz.append(fila, fb);
    return raiz;
  }

  const constructores = { mcq: montarMcq, num: montarNum };

  function montar(contenedor, defs, slug) {
    const raiz = typeof contenedor === "string" ? document.querySelector(contenedor) : contenedor;
    if (!raiz) throw new Error(`contenedor no encontrado: ${contenedor}`);
    const vistos = new Set();
    for (const def of defs) {
      if (vistos.has(def.id)) throw new Error(`id de ejercicio duplicado: ${def.id}`);
      vistos.add(def.id);
      const c = constructores[def.tipo];
      if (!c) throw new Error(`tipo de ejercicio sin constructor: ${def.tipo}`);
      raiz.append(c(def, slug));
    }
    vigilarQuiz(defs, slug);
  }

  /* El "quiz" de un tema son sus preguntas conceptuales. Cuando todas
     fueron respondidas, el tema queda apto para contar como completo.
     Sin esto, `quiz` nunca se pone en true y ningún tema llega a "completa". */
  function vigilarQuiz(defs, slug) {
    const ids = defs.filter(d => d.tipo === "mcq").map(d => d.id);
    if (!ids.length) { Progreso.marcarQuiz(slug); return; }
    const revisar = () => {
      const t = Progreso.leer().temas[slug];
      if (t && ids.every(id => id in (t.ejercicios || {}))) {
        Progreso.marcarQuiz(slug);
        removeEventListener("ejercicio-respondido", revisar);
      }
    };
    addEventListener("ejercicio-respondido", revisar);
  }
```

Y en `pintarResultado`, avisar que hubo respuesta:

```js
  function pintarResultado(caja, res) {
    caja.className = `ej-fb ${res.ok ? "ok" : "no"}`;
    caja.textContent = res.msg;
    caja.hidden = false;
    dispatchEvent(new CustomEvent("ejercicio-respondido"));
  }
```

- [ ] **Paso 6: Agregar los estilos de ejercicios a `shared.css`**

```css
/* ── Ejercicios ──────────────────────────────────────────── */
.ej {
  padding: var(--e6); margin-block: var(--e6);
  background: var(--panel); border: 1px solid var(--rule); border-radius: var(--radio);
}
.ej-q { font-weight: 500; margin-bottom: var(--e4); }
.ej-opts { display: grid; gap: var(--e2); }
.ej-opt {
  text-align: left; font: inherit; font-size: var(--t-small);
  padding: var(--e3) var(--e4); cursor: pointer;
  background: var(--bg); color: var(--ink);
  border: 1px solid var(--rule); border-radius: var(--radio);
  transition: border-color var(--transicion), background var(--transicion);
}
.ej-opt:hover:not(:disabled) { border-color: var(--accent); }
.ej-opt:disabled { cursor: default; }
.ej-opt.ok { border-color: var(--accent); background: var(--accent-tint); font-weight: 700; }
.ej-opt.no { opacity: 0.55; text-decoration: line-through; }

.ej-fila { display: flex; align-items: center; gap: var(--e3); flex-wrap: wrap; }
.ej-input {
  font-family: var(--font-mono); font-size: 1rem; width: 10rem;
  padding: var(--e2) var(--e3); background: var(--bg); color: var(--ink);
  border: 1px solid var(--rule); border-radius: var(--radio);
}
.ej-unidad { font-family: var(--font-mono); font-size: var(--t-small); color: var(--ink-mute); }
.ej-enviar {
  font: inherit; font-size: var(--t-small); font-weight: 700; cursor: pointer;
  padding: var(--e2) var(--e6); border-radius: 999px;
  background: var(--accent); color: var(--bg); border: 0;
}

.ej-fb {
  margin-top: var(--e4); padding: var(--e3) var(--e4);
  font-size: var(--t-small); border-radius: var(--radio);
  border-left: 3px solid var(--accent); background: var(--accent-tint);
}
.ej-fb.no { --fase-h: 25; }
.ej-fb.ok { --fase-h: 145; }
```

- [ ] **Paso 7: Volver a correr los tests**

Run: `node --test`
Expected: PASS, todos. El renderizado no se testea en Node — se prueba en la Tarea 12, en el navegador.

- [ ] **Paso 8: Commit**

```bash
git add ejercicios.js ejercicios.test.js shared.css
git commit -m "feat: correctores y renderizado de ejercicios MCQ y numéricos"
```

---

### Tarea 10: `ejercicios.js` — bloques Parsons

**Files:**
- Modify: `ejercicios.js`
- Modify: `ejercicios.test.js`
- Modify: `shared.css`

**Interfaces:**
- Consumes: `nodo()`, `pintarResultado()`, `constructores` de la Tarea 9.
- Produces: soporte del tipo
  ```js
  { tipo:"parsons", id:string, q:string, lineas:string[], distractores?:string[], expl:string }
  ```
  `corregir(def, arrayDeLineas)` compara texto e indentación en orden.

- [ ] **Paso 1: Escribir los tests que fallan**

Agregar a `ejercicios.test.js`:

```js
const parsons = {
  tipo: "parsons", id: "p1", q: "Armá el bucle",
  lineas: ["for i in range(n):", "    grad = derivada(w)", "    w -= lr * grad"],
  distractores: ["    w += lr * grad"],
  expl: "El gradiente apunta hacia arriba, por eso se resta."
};

test("parsons correcto da 100", () => {
  assert.strictEqual(E.corregir(parsons, parsons.lineas.slice()).puntaje, 100);
});

test("parsons con orden equivocado da parcial y no 100", () => {
  const r = E.corregir(parsons, ["    grad = derivada(w)", "for i in range(n):", "    w -= lr * grad"]);
  assert.strictEqual(r.ok, false);
  assert.ok(r.puntaje > 0 && r.puntaje < 100, `puntaje fuera de rango: ${r.puntaje}`);
});

test("parsons distingue la indentación", () => {
  const r = E.corregir(parsons, ["for i in range(n):", "grad = derivada(w)", "    w -= lr * grad"]);
  assert.strictEqual(r.ok, false);
  assert.match(r.msg, /indent/i);
});

test("parsons con un distractor usado no da 100", () => {
  const r = E.corregir(parsons, ["for i in range(n):", "    grad = derivada(w)", "    w += lr * grad"]);
  assert.strictEqual(r.ok, false);
});

test("parsons con cantidad de líneas equivocada no rompe", () => {
  const r = E.corregir(parsons, ["for i in range(n):"]);
  assert.strictEqual(r.ok, false);
  assert.ok(Number.isFinite(r.puntaje));
});
```

- [ ] **Paso 2: Correr y verificar que falla**

Run: `node --test ejercicios.test.js`
Expected: FAIL — `tipo de ejercicio desconocido: parsons`

- [ ] **Paso 3: Implementar el corrector**

Agregar dentro del IIFE de `ejercicios.js` y sumar el caso al `switch` de `corregir`:

```js
  function corregirParsons(def, dispuestas) {
    const esperadas = def.lineas;
    const n = esperadas.length;

    if (!Array.isArray(dispuestas) || dispuestas.length !== n) {
      return {
        ok: false, puntaje: 0,
        msg: `El algoritmo tiene ${n} líneas y pusiste ${Array.isArray(dispuestas) ? dispuestas.length : 0}.`
      };
    }

    let correctas = 0;
    let soloIndent = false;
    for (let i = 0; i < n; i++) {
      if (dispuestas[i] === esperadas[i]) { correctas++; continue; }
      if (dispuestas[i].trim() === esperadas[i].trim()) soloIndent = true;
    }

    if (correctas === n) return { ok: true, puntaje: 100, msg: def.expl };

    const puntaje = Math.round((correctas / n) * 100);
    const pista = soloIndent
      ? "Alguna línea tiene la indentación equivocada: en Python la sangría define qué está adentro del bucle."
      : "El orden no es el correcto todavía.";
    return { ok: false, puntaje, msg: `${correctas} de ${n} líneas en su lugar. ${pista}` };
  }
```

En `corregir`, agregar: `case "parsons": return corregirParsons(def, respuesta);`

- [ ] **Paso 4: Correr y verificar que pasa**

Run: `node --test ejercicios.test.js`
Expected: PASS, 13 tests.

- [ ] **Paso 5: Implementar el renderizado con arrastre y teclado**

Agregar al IIFE y registrar en `constructores`:

```js
  function montarParsons(def, slug) {
    const raiz = nodo("div", "ej ej-parsons");
    raiz.append(nodo("p", "ej-q", def.q));

    const lista = nodo("ol", "pa-lista");
    lista.setAttribute("aria-label", "Líneas de código: arrastralas o usá los botones para ordenarlas");

    // Mezcla determinística por id: la misma página da siempre el mismo desorden.
    const pool = [...def.lineas, ...(def.distractores || [])];
    const semilla = [...def.id].reduce((a, c) => a + c.charCodeAt(0), 0);
    pool.sort((a, b) => ((a.length * 31 + semilla) % 97) - ((b.length * 31 + semilla) % 97) || a.localeCompare(b));

    for (const texto of pool) lista.append(itemParsons(texto, lista));

    const boton = nodo("button", "ej-enviar", "Comprobar");
    boton.type = "button";
    const fb = nodo("div", "ej-fb");
    fb.hidden = true;

    boton.addEventListener("click", () => {
      const puestas = [...lista.querySelectorAll(".pa-item")]
        .filter(li => !li.classList.contains("descartada"))
        .map(li => li.dataset.linea);
      const res = corregirParsons(def, puestas);
      pintarResultado(fb, res);
      Progreso.registrar(slug, def.id, res.puntaje);
    });

    raiz.append(lista, boton, fb);
    return raiz;
  }

  function itemParsons(texto, lista) {
    const li = nodo("li", "pa-item");
    li.dataset.linea = texto;
    li.draggable = true;

    const pre = nodo("code", "pa-txt", texto);
    pre.style.paddingLeft = `${(texto.length - texto.trimStart().length) * 0.5}rem`;

    const ctrl = nodo("div", "pa-ctrl");
    const arriba = nodo("button", "pa-b", "↑");
    const abajo = nodo("button", "pa-b", "↓");
    const fuera = nodo("button", "pa-b", "×");
    arriba.type = abajo.type = fuera.type = "button";
    arriba.setAttribute("aria-label", "Subir esta línea");
    abajo.setAttribute("aria-label", "Bajar esta línea");
    fuera.setAttribute("aria-label", "Descartar esta línea, no va en el algoritmo");

    arriba.addEventListener("click", () => {
      const prev = li.previousElementSibling;
      if (prev) lista.insertBefore(li, prev);
    });
    abajo.addEventListener("click", () => {
      const sig = li.nextElementSibling;
      if (sig) lista.insertBefore(sig, li);
    });
    fuera.addEventListener("click", () => { li.classList.toggle("descartada"); });

    ctrl.append(arriba, abajo, fuera);
    li.append(pre, ctrl);

    li.addEventListener("dragstart", () => li.classList.add("arrastrando"));
    li.addEventListener("dragend", () => li.classList.remove("arrastrando"));
    return li;
  }
```

Y el manejador de `dragover` sobre la lista, dentro de `montarParsons` antes del `return`:

```js
    lista.addEventListener("dragover", (e) => {
      e.preventDefault();
      const arrastrando = lista.querySelector(".arrastrando");
      if (!arrastrando) return;
      const hermanos = [...lista.querySelectorAll(".pa-item:not(.arrastrando)")];
      const siguiente = hermanos.find(li => e.clientY < li.getBoundingClientRect().top + li.offsetHeight / 2);
      lista.insertBefore(arrastrando, siguiente || null);
    });
```

Registrar: `const constructores = { mcq: montarMcq, num: montarNum, parsons: montarParsons };`

Nota de accesibilidad: los botones ↑ ↓ × no son decorativos. El arrastre nativo de HTML5 no funciona con teclado ni de forma fiable en táctil, así que los botones son el camino principal de interacción, no un extra.

- [ ] **Paso 6: Estilos de Parsons en `shared.css`**

```css
/* ── Parsons ─────────────────────────────────────────────── */
.pa-lista { list-style: none; margin: 0 0 var(--e4); padding: 0; display: grid; gap: var(--e2); }
.pa-item {
  display: flex; align-items: center; justify-content: space-between; gap: var(--e4);
  padding: var(--e2) var(--e3); cursor: grab;
  background: var(--bg); border: 1px solid var(--rule); border-radius: var(--radio);
}
.pa-item.arrastrando { opacity: 0.4; border-color: var(--accent); }
.pa-item.descartada { opacity: 0.35; }
.pa-item.descartada .pa-txt { text-decoration: line-through; }
.pa-txt { font-family: var(--font-mono); font-size: var(--t-small); white-space: pre; }
.pa-ctrl { display: flex; gap: var(--e1); flex-shrink: 0; }
.pa-b {
  width: 26px; height: 26px; cursor: pointer; line-height: 1;
  background: none; color: var(--ink-mute);
  border: 1px solid var(--rule); border-radius: 6px;
}
.pa-b:hover { color: var(--accent); border-color: var(--accent); }
```

- [ ] **Paso 7: Commit**

```bash
git add ejercicios.js ejercicios.test.js shared.css
git commit -m "feat: ejercicios de bloques Parsons con arrastre y control por teclado"
```

---

### Tarea 11: `ejercicios.js` — Python vivo con Pyodide

**Files:**
- Modify: `ejercicios.js`
- Modify: `shared.css`

**Interfaces:**
- Consumes: `nodo()`, `pintarResultado()`, `constructores`.
- Produces: soporte del tipo
  ```js
  { tipo:"python", id:string, q:string, inicial:string,
    paquetes?:string[], esperado:string, expl:string }
  ```
  Y `Ejercicios.cargarPyodide(paquetes) → Promise<pyodide>`, con carga única y compartida entre ejercicios de la página.

- [ ] **Paso 1: Implementar el cargador**

Agregar al IIFE de `ejercicios.js`:

```js
  /* ── Pyodide, cargado una sola vez por página ─────────── */
  const PYODIDE_CDN = "https://cdn.jsdelivr.net/pyodide/v0.28.0/full/";
  let promesaPyodide = null;
  const paquetesCargados = new Set();

  async function cargarPyodide(paquetes = []) {
    if (!promesaPyodide) {
      promesaPyodide = (async () => {
        if (typeof loadPyodide === "undefined") {
          await new Promise((ok, err) => {
            const s = document.createElement("script");
            s.src = PYODIDE_CDN + "pyodide.js";
            s.onload = ok;
            s.onerror = () => err(new Error("no se pudo cargar Pyodide"));
            document.head.append(s);
          });
        }
        return loadPyodide({ indexURL: PYODIDE_CDN });
      })();
    }
    const py = await promesaPyodide;
    const faltantes = paquetes.filter(p => !paquetesCargados.has(p));
    if (faltantes.length) {
      await py.loadPackage(faltantes);
      faltantes.forEach(p => paquetesCargados.add(p));
    }
    return py;
  }
```

- [ ] **Paso 2: Implementar el ejercicio**

```js
  function montarPython(def, slug) {
    const raiz = nodo("div", "ej ej-python");
    raiz.append(nodo("p", "ej-q", def.q));

    const editor = nodo("textarea", "py-editor");
    editor.value = def.inicial;
    editor.spellcheck = false;
    editor.rows = Math.max(6, def.inicial.split("\n").length + 2);
    editor.setAttribute("aria-label", "Editor de Python");

    const fila = nodo("div", "ej-fila");
    const correr = nodo("button", "ej-enviar", "▶ Ejecutar");
    correr.type = "button";
    const reset = nodo("button", "diag-ctrl", "↻ Reiniciar");
    reset.type = "button";
    reset.addEventListener("click", () => { editor.value = def.inicial; });

    const estado = nodo("span", "py-estado");
    const salida = nodo("pre", "py-salida");
    salida.hidden = true;
    const fb = nodo("div", "ej-fb");
    fb.hidden = true;

    correr.addEventListener("click", async () => {
      correr.disabled = true;
      const pesado = (def.paquetes || []).length > 0;
      estado.textContent = paquetesCargados.size || promesaPyodide
        ? "Ejecutando…"
        : `Descargando Python${pesado ? " y sus paquetes" : ""}. Solo la primera vez.`;

      try {
        const py = await cargarPyodide(def.paquetes || []);
        estado.textContent = "Ejecutando…";

        // Capturar stdout y stderr sin ensuciar el intérprete del usuario.
        py.runPython(`
import sys, io
_buf = io.StringIO()
sys.stdout = _buf
sys.stderr = _buf
`);
        let error = null;
        try {
          await py.runPythonAsync(editor.value);
        } catch (e) {
          error = e;
        }
        const impreso = py.runPython("sys.stdout.getvalue()");
        py.runPython("sys.stdout = sys.__stdout__; sys.stderr = sys.__stderr__");

        salida.hidden = false;
        salida.textContent = error ? `${impreso}\n${error.message}` : (impreso || "(sin salida)");
        estado.textContent = "";

        if (error) {
          pintarResultado(fb, { ok: false, puntaje: 0, msg: "El código tiró un error. Leelo arriba: Python suele decir bastante bien qué pasó." });
          Progreso.registrar(slug, def.id, 0);
        } else {
          // Se compara la salida, no el código: hay muchas formas correctas de escribir lo mismo.
          const ok = impreso.trim() === def.esperado.trim();
          const res = ok
            ? { ok: true, puntaje: 100, msg: def.expl }
            : { ok: false, puntaje: 30, msg: `Corrió sin errores, pero la salida no es la esperada.\nEsperaba:\n${def.esperado}` };
          pintarResultado(fb, res);
          Progreso.registrar(slug, def.id, res.puntaje);
        }
      } catch (e) {
        estado.textContent = "";
        pintarResultado(fb, { ok: false, puntaje: 0, msg: `No se pudo cargar Python: ${e.message}. Probá recargar la página.` });
      } finally {
        correr.disabled = false;
      }
    });

    fila.append(correr, reset, estado);
    raiz.append(editor, fila, salida, fb);
    return raiz;
  }
```

Registrar: `const constructores = { mcq: montarMcq, num: montarNum, parsons: montarParsons, python: montarPython };`
Exportar también `cargarPyodide` en el `return` del IIFE.

- [ ] **Paso 3: Estilos**

```css
/* ── Python vivo ─────────────────────────────────────────── */
.py-editor {
  width: 100%; font-family: var(--font-mono); font-size: 0.875rem; line-height: 1.6;
  padding: var(--e4); margin-bottom: var(--e3); resize: vertical; tab-size: 4;
  background: var(--bg); color: var(--ink);
  border: 1px solid var(--rule); border-radius: var(--radio);
}
.py-salida {
  font-family: var(--font-mono); font-size: 0.8125rem; white-space: pre-wrap;
  margin-top: var(--e4); padding: var(--e4); max-height: 22rem; overflow: auto;
  background: var(--bg); border: 1px solid var(--rule); border-radius: var(--radio);
}
.py-estado { font-family: var(--font-mono); font-size: var(--t-small); color: var(--ink-mute); }
```

- [ ] **Paso 4: Probar a mano**

Agregar temporalmente a `muestra.html`:

```html
<div id="prueba-py"></div>
<script src="progreso.js"></script>
<script src="ejercicios.js"></script>
<script>
Ejercicios.montar("#prueba-py", [{
  tipo: "python", id: "tmp1", q: "Imprimí la suma de un array",
  inicial: "import numpy as np\nx = np.array([1, 2, 3, 4])\nprint(x.sum())",
  paquetes: ["numpy"], esperado: "10", expl: "NumPy suma el array entero de una."
}], "muestra");
</script>
```

Confirmar en el navegador:
- El mensaje de descarga aparece la primera vez y no la segunda (queda cacheado).
- La salida `10` marca correcto.
- Cambiar `.sum()` por `.mean()` marca incorrecto sin romperse.
- Un `SyntaxError` a propósito muestra el error de Python, no una pantalla rota.

Verificar la versión de Pyodide vigente en `https://pyodide.org/` y ajustar la URL del CDN si `v0.28.0` ya no es la actual.

Sacar el bloque de prueba de `muestra.html` antes de commitear.

- [ ] **Paso 5: Commit**

```bash
git add ejercicios.js shared.css
git commit -m "feat: ejercicios de Python real con Pyodide en demanda"
```

---

### Tarea 12: `concept-09-regresion-lineal.html` — el slice vertical

**Files:**
- Create: `concept-09-regresion-lineal.html`
- Create: `docs/fuentes/regresion-lineal.md`

**Interfaces:**
- Consumes: `shared.css`, `shared.js`, `anim.js`, `progreso.js`, `ejercicios.js`, y la clase `.fase-2`.
- Produces: la plantilla que replican los otros 23 temas. Su estructura es el contrato.

- [ ] **Paso 1: Extraer el material fuente**

Con `convert-documents-to-markdown`, extraer a `docs/fuentes/regresion-lineal.md`:
- ISLR, sección 3.1 completa (regresión lineal simple).
- ESL, sección 3.2 (mínimos cuadrados).
- Géron, capítulo 4 hasta la ecuación normal.

Es el insumo del contenido. No se despliega (`docs/fuentes/` está en `.gitignore`).

- [ ] **Paso 2: Escribir el esqueleto y el head**

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Regresión lineal simple · ML</title>
  <meta name="description" content="Cómo una recta aprende a predecir: mínimos cuadrados explicados desde cero, con ejercicios.">
  <link rel="stylesheet" href="shared.css">
  <script src="shared.js"></script>
</head>
<body class="fase-2">
  <div class="barra-progreso"></div>
  <nav>
    <a href="index.html" class="nav-brand">ML · Estudio</a>
    <div class="nav-right">
      <a href="index.html" class="nav-link">Inicio</a>
      <a href="temas.html" class="nav-link">Temas</a>
      <!-- Estudio y Práctica se agregan en la Etapa 6, cuando esas páginas
           existan. Un link de navegación a un 404 es peor que su ausencia. -->
      <button class="theme-tog" id="theme-tog" onclick="toggleTheme()" aria-label="Cambiar tema">☾</button>
    </div>
  </nav>
  <main class="envoltura">
    <!-- contenido de los pasos siguientes -->
  </main>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.13.0/dist/gsap.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.13.0/dist/ScrollTrigger.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.13.0/dist/DrawSVGPlugin.min.js"></script>
  <script src="anim.js"></script>
  <script src="progreso.js"></script>
  <script src="ejercicios.js"></script>
  <script src="regresion-lineal.js"></script>
</body>
</html>
```

La clase `.fase-2` en el `<body>` es lo que fija el acento verde de toda la página. Ninguna otra regla de color se escribe.

- [ ] **Paso 3: Escribir el hero y la sección de intuición**

```html
<header class="hero-c">
  <p class="breadcrumb"><a href="index.html">Inicio</a> · <a href="temas.html">Temas</a> · Fase 2</p>
  <p class="eyebrow">Tema 09 · Regresión</p>
  <h1>Regresión lineal simple</h1>
  <p class="lede">La forma más vieja y más útil de que una máquina aprenda: encontrar la recta que menos se equivoca.</p>
  <hr class="regla">
</header>

<section class="section reveal">
  <div class="sec-no">01</div>
  <div class="sec-body">
    <h2>La intuición, sin una sola fórmula</h2>
    <p>Tenés puntos en un gráfico…</p>
    <figure class="diagram" id="d-intuicion">
      <!-- SVG: nube de puntos, varias rectas candidatas, la mejor destacada -->
    </figure>
  </div>
</section>
```

Regla de contenido de la sección 01: explicar la idea completa **sin ninguna notación matemática**. Si David o Abel no la entienden acá, el resto del tema no los va a rescatar.

- [ ] **Paso 4: Escribir las secciones 02 a 05 (el mecanismo)**

- **02 · El error de una recta** — qué es un residuo, por qué se elevan al cuadrado en vez de tomar valor absoluto. Diagrama: los residuos como segmentos verticales, y los cuadrados literales sobre ellos.
- **03 · Mínimos cuadrados** — la fórmula cerrada, cada término explicado por separado:
  ```html
  <div class="callout formula">β₁ = Σ(xᵢ − x̄)(yᵢ − ȳ) / Σ(xᵢ − x̄)²  &nbsp;&nbsp; β₀ = ȳ − β₁x̄</div>
  ```
  Después de la fórmula, un párrafo por símbolo. Nada de "donde β₁ es la pendiente" y seguir: explicar *por qué* el numerador es una covarianza y el denominador una varianza.
- **04 · Calcularlo a mano** — una tabla de 5 puntos, resuelta columna por columna. Este es el material que después usan los ejercicios numéricos.
- **05 · Qué tan buena es la recta** — R², y un `.callout.warn` sobre por qué un R² alto no significa que el modelo sirva.

Cada sección lleva `class="section reveal"` y su `.sec-no` correlativo.

- [ ] **Paso 5: Escribir el diagrama principal con secuencia fijada**

Sección 06, el momento donde el proceso *es* el concepto. Un SVG donde la recta arranca mal y va bajando el error mientras el usuario scrollea.

```html
<section class="section" id="s-ajuste">
  <div class="sec-no">06</div>
  <div class="sec-body">
    <h2>Ver el ajuste ocurriendo</h2>
    <figure class="diagram" id="d-ajuste">
      <svg viewBox="0 0 640 380" role="img"
           aria-label="Una recta candidata gira y se desplaza hasta minimizar la suma de los residuos al cuadrado.">
        <line class="daxis" x1="60" y1="330" x2="600" y2="330"/>
        <line class="daxis" x1="60" y1="330" x2="60" y2="40"/>
        <g id="puntos"><!-- 8 <circle class="dnode-solid" r="5"> --></g>
        <g id="residuos"><!-- 8 <line class="dedge-mute"> --></g>
        <line id="recta" class="dedge" x1="60" y1="300" x2="600" y2="280"/>
        <text class="dlabel" x="330" y="365" text-anchor="middle">superficie de m2</text>
        <text class="dlabel" x="20" y="185" text-anchor="middle" transform="rotate(-90 20 185)">precio</text>
        <text class="dtxt-mute" id="etq-error" x="600" y="60" text-anchor="end">error: —</text>
      </svg>
    </figure>
  </div>
</section>
```

En `regresion-lineal.js`:

```js
Anim.secuencia("#s-ajuste", [
  { el: "#recta", desde: { attr: { y1: 300, y2: 280 } }, hasta: { attr: { y1: 290, y2: 150 } } },
  { el: "#recta", desde: { attr: { y1: 290, y2: 150 } }, hasta: { attr: { y1: 305, y2: 105 } } },
  { el: "#recta", desde: { attr: { y1: 305, y2: 105 } }, hasta: { attr: { y1: 300, y2: 120 } } }
]);
Anim.dibujar("#d-intuicion");
Anim.aparecer("#d-intuicion");
```

El `aria-label` del `<svg>` describe lo que pasa, no lo que se ve quieto: es lo único que recibe alguien con lector de pantalla.

- [ ] **Paso 6: Escribir el simulador interactivo**

Sección 07. Dos sliders (pendiente e intercepto) que mueven la recta y recalculan el error en vivo. Sin GSAP: es manipulación directa del DOM en respuesta a un `input`.

```html
<section class="section" id="s-sim">
  <div class="sec-no">07</div>
  <div class="sec-body">
    <h2>Probá vos</h2>
    <p>Movés la recta, el error se recalcula. Buscá el mínimo a mano y después comparalo con lo que da la fórmula.</p>
    <figure class="diagram">
      <svg viewBox="0 0 640 340" id="sim-svg" role="img" aria-label="Simulador de ajuste de recta">
        <line class="daxis" x1="60" y1="290" x2="600" y2="290"/>
        <line class="daxis" x1="60" y1="290" x2="60" y2="30"/>
        <g id="sim-puntos"></g>
        <g id="sim-residuos"></g>
        <line id="sim-recta" class="dedge" x1="60" y1="0" x2="600" y2="0"/>
      </svg>
      <div class="sim-ctrl">
        <label>Pendiente <input type="range" id="sim-b1" min="-2" max="6" step="0.1" value="1"><output id="out-b1">1.0</output></label>
        <label>Intercepto <input type="range" id="sim-b0" min="-40" max="80" step="1" value="0"><output id="out-b0">0</output></label>
        <p class="sim-error">Suma de cuadrados: <strong id="sim-sse">—</strong></p>
      </div>
    </figure>
  </div>
</section>
```

```js
/* Simulador: sin animación, respuesta directa al input. */
const DATOS = [[35, 62], [48, 88], [55, 96], [62, 118], [70, 128], [78, 152], [85, 160], [95, 190]];
const aX = m2 => 60 + (m2 - 30) * 7.7;
const aY = p  => 290 - (p - 50) * 1.7;

function pintarSim() {
  const b1 = parseFloat(document.getElementById("sim-b1").value);
  const b0 = parseFloat(document.getElementById("sim-b0").value);
  document.getElementById("out-b1").textContent = b1.toFixed(1);
  document.getElementById("out-b0").textContent = b0;

  const pred = m2 => b0 + b1 * m2;
  const recta = document.getElementById("sim-recta");
  recta.setAttribute("x1", aX(30)); recta.setAttribute("y1", aY(pred(30)));
  recta.setAttribute("x2", aX(100)); recta.setAttribute("y2", aY(pred(100)));

  const g = document.getElementById("sim-residuos");
  g.replaceChildren();
  let sse = 0;
  for (const [m2, precio] of DATOS) {
    const e = precio - pred(m2);
    sse += e * e;
    const l = document.createElementNS("http://www.w3.org/2000/svg", "line");
    l.setAttribute("class", "dedge-mute");
    l.setAttribute("x1", aX(m2)); l.setAttribute("y1", aY(precio));
    l.setAttribute("x2", aX(m2)); l.setAttribute("y2", aY(pred(m2)));
    g.append(l);
  }
  document.getElementById("sim-sse").textContent = Math.round(sse).toLocaleString("es-AR");
}

function pintarPuntos() {
  const g = document.getElementById("sim-puntos");
  for (const [m2, precio] of DATOS) {
    const c = document.createElementNS("http://www.w3.org/2000/svg", "circle");
    c.setAttribute("class", "dnode-solid");
    c.setAttribute("cx", aX(m2)); c.setAttribute("cy", aY(precio)); c.setAttribute("r", 5);
    g.append(c);
  }
}

pintarPuntos();
for (const id of ["sim-b1", "sim-b0"]) {
  document.getElementById(id).addEventListener("input", pintarSim);
}
pintarSim();
```

Estilos para `.sim-ctrl` en `shared.css`:

```css
.sim-ctrl { display: grid; gap: var(--e3); margin-top: var(--e6); }
.sim-ctrl label { display: grid; grid-template-columns: 7rem 1fr 3.5rem; align-items: center; gap: var(--e3); font-size: var(--t-small); }
.sim-ctrl output { font-family: var(--font-mono); text-align: right; }
.sim-ctrl input[type="range"] { accent-color: var(--accent); }
.sim-error { font-size: var(--t-small); color: var(--ink-mute); }
.sim-error strong { font-family: var(--font-mono); color: var(--accent); }
```

- [ ] **Paso 7: Escribir los ejercicios**

Sección 08, con la progresión completa. En `regresion-lineal.js`:

```js
Ejercicios.montar("#ejercicios", [
  { tipo: "mcq", id: "rl-c1",
    q: "¿Por qué los errores se elevan al cuadrado en vez de sumarse tal cual?",
    opts: [
      "Porque el cuadrado es más fácil de calcular",
      "Porque si no, los errores positivos y negativos se cancelan entre sí",
      "Porque así el resultado siempre da un número entero",
      "Porque los errores negativos no existen"
    ],
    c: 1,
    expl: "Un punto 10 arriba y otro 10 abajo darían error total cero, y la recta parecería perfecta siendo mala. El cuadrado los hace sumar, y de paso castiga más los errores grandes." },

  { tipo: "num", id: "rl-n1",
    q: "Con los puntos (1,2), (2,4), (3,5), (4,9): ¿cuánto vale la pendiente β₁? Redondeá a dos decimales.",
    resp: 2.10, tol: 0.02,
    trampas: [
      { val: 5.0, msg: "Ese es el promedio de los y, no la pendiente. La pendiente relaciona cómo cambia y cuando cambia x." },
      { val: 0.48, msg: "Diste vuelta la fracción: el denominador es la variación de x, no la de y." }
    ],
    expl: "x̄ = 2.5, ȳ = 5. Numerador Σ(xᵢ−x̄)(yᵢ−ȳ) = 10.5, denominador Σ(xᵢ−x̄)² = 5. Da 2.1." },

  { tipo: "num", id: "rl-n2",
    q: "Con la misma pendiente, ¿cuánto vale el intercepto β₀?",
    resp: -0.25, tol: 0.02,
    trampas: [{ val: 0.25, msg: "El signo. β₀ = ȳ − β₁x̄, y β₁x̄ es más grande que ȳ acá." }],
    expl: "β₀ = 5 − 2.1 × 2.5 = −0.25." },

  { tipo: "parsons", id: "rl-p1",
    q: "Armá la función que calcula la pendiente. Sobra una línea: descartala con ×.",
    lineas: [
      "def pendiente(x, y):",
      "    mx = sum(x) / len(x)",
      "    my = sum(y) / len(y)",
      "    num = sum((xi - mx) * (yi - my) for xi, yi in zip(x, y))",
      "    den = sum((xi - mx) ** 2 for xi in x)",
      "    return num / den"
    ],
    distractores: ["    den = sum((yi - my) ** 2 for yi in y)"],
    expl: "El denominador siempre lleva la variación de x. Si usás la de y, estás calculando otra cosa." },

  { tipo: "python", id: "rl-py1",
    q: "Ajustá la misma recta con scikit-learn y mostrá pendiente e intercepto redondeados a dos decimales.",
    inicial: `from sklearn.linear_model import LinearRegression
import numpy as np

X = np.array([[1], [2], [3], [4]])
y = np.array([2, 4, 5, 9])

modelo = LinearRegression()
# Completá: entrenalo y mostrá coef_ e intercept_
`,
    paquetes: ["scikit-learn", "numpy"],
    esperado: "2.1 -0.25",
    expl: "Tres líneas de sklearn dan lo mismo que calculaste a mano. Eso es lo que hace la librería: la fórmula que ya sabés." }
], "regresion-lineal");
```

- [ ] **Paso 8: Cerrar con errores frecuentes, síntesis y navegación**

```html
<section class="section reveal">
  <div class="sec-no">09</div>
  <div class="sec-body">
    <h2>Errores frecuentes</h2>
    <div class="callout warn"><span class="etiqueta">Trampa</span>
      Correlación no es causalidad…</div>
    <div class="callout warn"><span class="etiqueta">Trampa</span>
      Extrapolar fuera del rango de los datos…</div>
    <div class="callout err"><span class="etiqueta">Error clásico</span>
      Un R² alto con un modelo inútil…</div>
  </div>
</section>

<section class="section reveal" id="sintesis">
  <div class="sec-no">10</div>
  <div class="sec-body">
    <h2>Síntesis</h2>
    <p>[80 a 120 palabras. Se reutiliza tal cual en estudio.html.]</p>
  </div>
</section>

<nav class="concept-nav">
  <a href="concept-08-sesgo-varianza.html"><span class="cn-dir">← Anterior</span><span class="cn-t">Sesgo, varianza y overfitting</span></a>
  <a href="concept-10-gradiente.html" class="cn-next"><span class="cn-dir">Siguiente →</span><span class="cn-t">Regresión múltiple y descenso de gradiente</span></a>
</nav>
```

Y al final de `regresion-lineal.js`, marcar leído cuando el usuario llega abajo de verdad:

```js
/* "Leído" se marca al llegar a la síntesis, no al abrir la página. */
new IntersectionObserver((es, obs) => {
  if (es[0].isIntersecting) {
    Progreso.marcarLeido("regresion-lineal");
    obs.disconnect();
  }
}, { threshold: 0.6 }).observe(document.getElementById("sintesis"));
```

- [ ] **Paso 9: Verificar**

Run: `node verificar.js`
Expected: `✓ verificación OK` — en particular, ningún hexadecimal dentro de los `<svg>`.

En el navegador, recorrer la página entera y confirmar:
- Tema claro y oscuro, ambos legibles de arriba a abajo.
- Con movimiento reducido activado: el diagrama de la sección 06 muestra la recta ajustada, no una recta random ni un SVG vacío.
- En ancho de teléfono: la secuencia fijada no se fija (degrada), los sliders se pueden usar con el pulgar.
- Cada ejercicio respondido mal a propósito da un mensaje que **diagnostica**, no solo "incorrecto".
- Recargar después de responder: el progreso persiste (chequear `localStorage` en el inspector).

- [ ] **Paso 10: Commit**

```bash
git add concept-09-regresion-lineal.html regresion-lineal.js shared.css
git commit -m "feat: tema de regresión lineal completo, slice vertical del patrón"
```

- [ ] **Paso 11: CHECKPOINT — que lo usen David y Abel**

Detener la implementación acá. Mandarles la página. Preguntar concretamente:
1. ¿Entendieron la sección 01 sin leer el resto?
2. ¿Pudieron hacer el ejercicio numérico sin ayuda?
3. ¿El de Python los frustró o los enganchó?
4. ¿Qué parte volvieron a leer dos veces?

Lo que salga de acá corrige la plantilla **antes** de replicarla 23 veces. Este es el punto entero de haber hecho un slice vertical: no seguir hasta tener la respuesta.

---

# ETAPA 4 — Cáscara y persistencia

### Tarea 13: `api/progress.js` — verificación del token de Google

**Files:**
- Create: `api/progress.js`
- Create: `api/progress.test.js`

**Interfaces:**
- Consumes: `node:crypto`, `fetch` global.
- Produces:
  - `verificarToken(token, clientId, allowlist) → Promise<payload>` — tira si el token es inválido.
  - `module.exports.handler` — el handler de Vercel.
  - Variables de entorno: `GOOGLE_CLIENT_ID`, `EMAILS_PERMITIDOS` (separados por coma), y el par de Redis.

- [ ] **Paso 1: Escribir los tests que fallan**

Crear `api/progress.test.js`. Los tests generan un par de claves RSA real y firman tokens, así que verifican la firma de verdad sin salir a la red.

```js
const { test } = require("node:test");
const assert = require("node:assert");
const crypto = require("node:crypto");
const { verificarToken, _setClavesParaTest } = require("./progress.js");

const { publicKey, privateKey } = crypto.generateKeyPairSync("rsa", { modulusLength: 2048 });
const jwk = { ...publicKey.export({ format: "jwk" }), kid: "test-kid", alg: "RS256", use: "sig" };

const b64u = (obj) => Buffer.from(JSON.stringify(obj)).toString("base64url");

function firmar(payload, { kid = "test-kid", alg = "RS256" } = {}) {
  const cabeza = b64u({ alg, kid, typ: "JWT" });
  const cuerpo = b64u(payload);
  const firma = crypto.sign("RSA-SHA256", Buffer.from(`${cabeza}.${cuerpo}`), privateKey);
  return `${cabeza}.${cuerpo}.${firma.toString("base64url")}`;
}

const CLIENT_ID = "cliente-de-prueba.apps.googleusercontent.com";
const PERMITIDOS = ["david@gmail.com", "abel@gmail.com"];
const futuro = () => Math.floor(Date.now() / 1000) + 3600;

const valido = () => ({
  iss: "https://accounts.google.com", aud: CLIENT_ID, sub: "1234567890",
  email: "david@gmail.com", email_verified: true, exp: futuro()
});

test.before(() => { _setClavesParaTest([jwk]); });

test("acepta un token válido y devuelve el payload", async () => {
  const p = await verificarToken(firmar(valido()), CLIENT_ID, PERMITIDOS);
  assert.strictEqual(p.sub, "1234567890");
});

test("rechaza una firma adulterada", async () => {
  const t = firmar(valido());
  const roto = t.slice(0, -6) + "AAAAAA";
  await assert.rejects(() => verificarToken(roto, CLIENT_ID, PERMITIDOS), /firma/);
});

test("rechaza un token con el payload cambiado después de firmar", async () => {
  const [c, , f] = firmar(valido()).split(".");
  const otro = b64u({ ...valido(), email: "intruso@gmail.com" });
  await assert.rejects(() => verificarToken(`${c}.${otro}.${f}`, CLIENT_ID, PERMITIDOS), /firma/);
});

test("rechaza un token expirado", async () => {
  const t = firmar({ ...valido(), exp: Math.floor(Date.now() / 1000) - 10 });
  await assert.rejects(() => verificarToken(t, CLIENT_ID, PERMITIDOS), /expirado/);
});

test("rechaza otro aud", async () => {
  const t = firmar({ ...valido(), aud: "otra-app.apps.googleusercontent.com" });
  await assert.rejects(() => verificarToken(t, CLIENT_ID, PERMITIDOS), /aud/);
});

test("rechaza otro iss", async () => {
  const t = firmar({ ...valido(), iss: "https://evil.example.com" });
  await assert.rejects(() => verificarToken(t, CLIENT_ID, PERMITIDOS), /iss/);
});

test("rechaza alg none", async () => {
  const cabeza = b64u({ alg: "none", kid: "test-kid", typ: "JWT" });
  const cuerpo = b64u(valido());
  await assert.rejects(() => verificarToken(`${cabeza}.${cuerpo}.`, CLIENT_ID, PERMITIDOS), /alg/);
});

test("rechaza un email fuera de la allowlist", async () => {
  const t = firmar({ ...valido(), email: "cualquiera@gmail.com" });
  await assert.rejects(() => verificarToken(t, CLIENT_ID, PERMITIDOS), /autorizado/);
});

test("rechaza un email sin verificar", async () => {
  const t = firmar({ ...valido(), email_verified: false });
  await assert.rejects(() => verificarToken(t, CLIENT_ID, PERMITIDOS), /autorizado/);
});

test("rechaza un kid desconocido", async () => {
  const t = firmar(valido(), { kid: "kid-que-no-existe" });
  await assert.rejects(() => verificarToken(t, CLIENT_ID, PERMITIDOS), /kid/);
});

test("rechaza basura que no es un JWT", async () => {
  await assert.rejects(() => verificarToken("no-soy-un-token", CLIENT_ID, PERMITIDOS), /formato/);
});
```

- [ ] **Paso 2: Correr y verificar que falla**

Run: `node --test api/progress.test.js`
Expected: FAIL — `Cannot find module './progress.js'`

- [ ] **Paso 3: Implementar la verificación**

Crear `api/progress.js`:

```js
const crypto = require("node:crypto");

const CERTS_URL = "https://www.googleapis.com/oauth2/v3/certs";
const ISS_VALIDOS = ["accounts.google.com", "https://accounts.google.com"];

let cacheClaves = { claves: null, vence: 0 };

/* Solo para tests: inyecta claves y saltea la red. */
function _setClavesParaTest(claves) {
  cacheClaves = { claves, vence: Date.now() + 3600e3 };
}

async function clavesGoogle() {
  if (cacheClaves.claves && Date.now() < cacheClaves.vence) return cacheClaves.claves;
  const r = await fetch(CERTS_URL);
  if (!r.ok) throw new Error("no se pudieron traer las claves de Google");
  const m = /max-age=(\d+)/.exec(r.headers.get("cache-control") || "");
  const { keys } = await r.json();
  cacheClaves = { claves: keys, vence: Date.now() + (m ? Number(m[1]) : 3600) * 1000 };
  return keys;
}

const desdeB64u = (s) => Buffer.from(s, "base64url");

async function verificarToken(token, clientId, allowlist) {
  const partes = String(token || "").split(".");
  if (partes.length !== 3) throw new Error("formato de token inválido");
  const [c64, p64, f64] = partes;

  let cabeza;
  try { cabeza = JSON.parse(desdeB64u(c64).toString("utf8")); }
  catch { throw new Error("formato de token inválido"); }

  // Se chequea antes que nada: un alg "none" con firma vacía no debe llegar ni a mirarse.
  if (cabeza.alg !== "RS256") throw new Error(`alg no soportado: ${cabeza.alg}`);

  const jwk = (await clavesGoogle()).find(k => k.kid === cabeza.kid);
  if (!jwk) throw new Error("kid desconocido");

  const publica = crypto.createPublicKey({ key: jwk, format: "jwk" });
  const firmaOk = crypto.verify(
    "RSA-SHA256", Buffer.from(`${c64}.${p64}`), publica, desdeB64u(f64)
  );
  if (!firmaOk) throw new Error("firma inválida");

  let payload;
  try { payload = JSON.parse(desdeB64u(p64).toString("utf8")); }
  catch { throw new Error("formato de token inválido"); }

  if (!payload.exp || payload.exp <= Math.floor(Date.now() / 1000)) throw new Error("token expirado");
  if (payload.aud !== clientId) throw new Error("aud incorrecto");
  if (!ISS_VALIDOS.includes(payload.iss)) throw new Error("iss incorrecto");
  if (!payload.email_verified || !allowlist.includes(payload.email)) {
    throw new Error("usuario no autorizado");
  }
  return payload;
}

module.exports = { verificarToken, _setClavesParaTest };
```

- [ ] **Paso 4: Correr y verificar que pasa**

Run: `node --test api/progress.test.js`
Expected: PASS, 11 tests.

Los tres tests que importan de verdad son el de firma adulterada, el de payload cambiado y el de `alg: none`. Son los tres ataques reales contra un endpoint que acepta JWTs. Si alguno de esos pasa cuando no debería, el endpoint es escribible por cualquiera.

- [ ] **Paso 5: Commit**

```bash
git add api/progress.js api/progress.test.js
git commit -m "feat: verificación de ID tokens de Google con crypto de la stdlib"
```

---

### Tarea 14: `api/progress.js` — Redis y el handler

**Files:**
- Modify: `api/progress.js`
- Modify: `api/progress.test.js`

**Interfaces:**
- Consumes: `verificarToken` de la Tarea 13.
- Produces: el handler por defecto de la función. Contrato:
  - `GET  /api/progress` con `Authorization: Bearer <id_token>` → `200 {json}` | `204` | `401`
  - `POST /api/progress` con el mismo header y el estado en el cuerpo → `200 {ok:true}` | `401` | `413`

- [ ] **Paso 1: Escribir el test del límite de tamaño**

Agregar a `api/progress.test.js`:

```js
const { _validarCuerpo } = require("./progress.js");

test("_validarCuerpo acepta un estado normal", () => {
  const est = { v: 1, temas: { rl: { leido: true, quiz: true, ejercicios: { e1: 100 } } } };
  assert.strictEqual(_validarCuerpo(est).ok, true);
});

test("_validarCuerpo rechaza lo que no es un estado", () => {
  assert.strictEqual(_validarCuerpo(null).ok, false);
  assert.strictEqual(_validarCuerpo("hola").ok, false);
  assert.strictEqual(_validarCuerpo({ sin: "temas" }).ok, false);
  assert.strictEqual(_validarCuerpo({ v: 1, temas: [] }).ok, false);
});

test("_validarCuerpo rechaza un cuerpo demasiado grande", () => {
  const gordo = { v: 1, temas: {} };
  for (let i = 0; i < 20000; i++) gordo.temas["t" + i] = { leido: true, quiz: true, ejercicios: {} };
  const r = _validarCuerpo(gordo);
  assert.strictEqual(r.ok, false);
  assert.strictEqual(r.codigo, 413);
});
```

- [ ] **Paso 2: Correr y verificar que falla**

Run: `node --test api/progress.test.js`
Expected: FAIL — `_validarCuerpo is not a function`

- [ ] **Paso 3: Implementar Redis, la validación y el handler**

Agregar a `api/progress.js`:

```js
const LIMITE_BYTES = 64 * 1024;

function _validarCuerpo(cuerpo) {
  if (!cuerpo || typeof cuerpo !== "object" || Array.isArray(cuerpo)) {
    return { ok: false, codigo: 400, msg: "cuerpo inválido" };
  }
  if (!cuerpo.temas || typeof cuerpo.temas !== "object" || Array.isArray(cuerpo.temas)) {
    return { ok: false, codigo: 400, msg: "falta el objeto temas" };
  }
  if (Buffer.byteLength(JSON.stringify(cuerpo), "utf8") > LIMITE_BYTES) {
    return { ok: false, codigo: 413, msg: "el progreso excede 64 KB" };
  }
  return { ok: true };
}

/* Upstash por REST: sin cliente de Redis, solo fetch. */
function configRedis() {
  const url = process.env.KV_REST_API_URL || process.env.UPSTASH_REDIS_REST_URL;
  const token = process.env.KV_REST_API_TOKEN || process.env.UPSTASH_REDIS_REST_TOKEN;
  if (!url || !token) throw new Error("faltan las variables de entorno de Redis");
  return { url: url.replace(/\/$/, ""), token };
}

async function redisGet(clave) {
  const { url, token } = configRedis();
  const r = await fetch(`${url}/get/${encodeURIComponent(clave)}`, {
    headers: { Authorization: `Bearer ${token}` }
  });
  if (!r.ok) throw new Error(`Redis GET falló: ${r.status}`);
  const { result } = await r.json();
  return result ? JSON.parse(result) : null;
}

async function redisSet(clave, valor) {
  const { url, token } = configRedis();
  const r = await fetch(`${url}/set/${encodeURIComponent(clave)}`, {
    method: "POST",
    headers: { Authorization: `Bearer ${token}`, "Content-Type": "text/plain" },
    body: JSON.stringify(valor)
  });
  if (!r.ok) throw new Error(`Redis SET falló: ${r.status}`);
}

async function handler(req, res) {
  const clientId = process.env.GOOGLE_CLIENT_ID;
  const permitidos = (process.env.EMAILS_PERMITIDOS || "").split(",").map(s => s.trim()).filter(Boolean);
  if (!clientId || !permitidos.length) {
    return res.status(500).json({ error: "servidor mal configurado" });
  }

  const auth = req.headers.authorization || "";
  const token = auth.startsWith("Bearer ") ? auth.slice(7) : null;
  if (!token) return res.status(401).json({ error: "falta el token" });

  let usuario;
  try {
    usuario = await verificarToken(token, clientId, permitidos);
  } catch (e) {
    // Sin detalle al cliente: no le decimos a un atacante qué chequeo falló.
    console.warn("token rechazado:", e.message);
    return res.status(401).json({ error: "no autorizado" });
  }

  const clave = `ml:progress:${usuario.sub}`;

  try {
    if (req.method === "GET") {
      const datos = await redisGet(clave);
      return datos ? res.status(200).json(datos) : res.status(204).end();
    }

    if (req.method === "POST") {
      const cuerpo = typeof req.body === "string" ? JSON.parse(req.body) : req.body;
      const v = _validarCuerpo(cuerpo);
      if (!v.ok) return res.status(v.codigo).json({ error: v.msg });
      await redisSet(clave, cuerpo);
      return res.status(200).json({ ok: true });
    }

    res.setHeader("Allow", "GET, POST");
    return res.status(405).json({ error: "método no permitido" });
  } catch (e) {
    console.error("error del almacenamiento:", e);
    return res.status(500).json({ error: "no se pudo acceder al almacenamiento" });
  }
}

module.exports = handler;
module.exports.verificarToken = verificarToken;
module.exports._setClavesParaTest = _setClavesParaTest;
module.exports._validarCuerpo = _validarCuerpo;
```

Nota sobre la fusión: el servidor guarda lo que le mandan, sin fusionar. La fusión ocurre en el cliente (Tarea 15), que trae el remoto, lo fusiona con el local y manda el resultado. Con un solo usuario por cuenta no hay escrituras concurrentes reales, y poner la fusión de un solo lado evita tener dos implementaciones que se puedan desincronizar.

- [ ] **Paso 4: Correr y verificar que pasa**

Run: `node --test`
Expected: PASS, todos los archivos de test.

- [ ] **Paso 5: Commit**

```bash
git add api/progress.js api/progress.test.js
git commit -m "feat: handler de progreso con Redis por REST y límite de tamaño"
```

---

### Tarea 15: Login con Google y sincronización

**Files:**
- Modify: `progreso.js` (agregar el módulo `Sync`)
- Modify: `shared.css`
- Create: `.env.example`

**Interfaces:**
- Consumes: `Progreso.fusionar`, `Progreso.leer`, `Progreso.guardar`; el endpoint de la Tarea 14.
- Produces: la global `Sync` con:
  - `Sync.montar(selectorContenedor, clientId) → void` — dibuja el botón de Google o el estado de sesión.
  - `Sync.encolar(estado) → void` — POST con debounce de 2 s. La llama `Progreso.guardar`.
  - `Sync.entrar(credential) → Promise<void>` — trae el remoto, fusiona, guarda, sube.
  - `Sync.salir() → void`

- [ ] **Paso 1: Escribir `.env.example`**

```bash
# Google Cloud Console → APIs y servicios → Credenciales → ID de cliente de OAuth (app web)
# Orígenes autorizados: http://localhost:3000 y la URL de producción de Vercel
GOOGLE_CLIENT_ID=xxxxxxxx.apps.googleusercontent.com

# Los tres correos que pueden guardar progreso, separados por coma
EMAILS_PERMITIDOS=david@gmail.com,abel@gmail.com,ocamposimon1@gmail.com

# Los pone solo la integración de Upstash desde el Marketplace de Vercel
KV_REST_API_URL=
KV_REST_API_TOKEN=
```

`GOOGLE_CLIENT_ID` no es secreto — va también embebido en el HTML del cliente. Los tokens de Redis sí lo son y nunca salen del servidor.

- [ ] **Paso 2: Implementar `Sync` al final de `progreso.js`**

```js
/* ── Sincronización con el servidor ──────────────────────── */
const Sync = (() => {
  const CLAVE_CRED = "ml-cred";
  let cred = null;
  let temporizador = null;

  const sesionActiva = () => Boolean(cred);

  async function pedir(metodo, cuerpo) {
    const r = await fetch("/api/progress", {
      method: metodo,
      headers: {
        Authorization: `Bearer ${cred}`,
        ...(cuerpo ? { "Content-Type": "application/json" } : {})
      },
      ...(cuerpo ? { body: JSON.stringify(cuerpo) } : {})
    });
    if (r.status === 401) { salir(); throw new Error("sesión vencida"); }
    if (!r.ok && r.status !== 204) throw new Error(`el servidor respondió ${r.status}`);
    return r.status === 204 ? null : r.json();
  }

  /* Debounce: escribir un ejercicio no dispara un POST por tecla. */
  function encolar(estado) {
    if (!sesionActiva()) return;
    clearTimeout(temporizador);
    temporizador = setTimeout(() => {
      // Si falla, no se avisa: el dato ya está en localStorage y el próximo
      // guardado lo reintenta. No vale la pena una cola de reintentos acá.
      pedir("POST", estado).catch(e => console.warn("no se pudo sincronizar:", e.message));
    }, 2000);
  }

  async function entrar(credential) {
    cred = credential;
    sessionStorage.setItem(CLAVE_CRED, credential);
    try {
      const remoto = await pedir("GET");
      const fusionado = Progreso.fusionar(Progreso.leer(), remoto);
      Progreso.guardar(fusionado);           // guardar() vuelve a llamar a encolar()
      await pedir("POST", fusionado);        // y además sube ya, sin esperar el debounce
      pintar();
      dispatchEvent(new CustomEvent("progreso-actualizado"));
    } catch (e) {
      console.warn("no se pudo traer el progreso remoto:", e.message);
      pintar();
    }
  }

  function salir() {
    cred = null;
    sessionStorage.removeItem(CLAVE_CRED);
    if (window.google?.accounts?.id) google.accounts.id.disableAutoSelect();
    pintar();
  }

  let contenedor = null;
  let clientIdGuardado = null;

  function pintar() {
    if (!contenedor) return;
    contenedor.replaceChildren();
    if (sesionActiva()) {
      const p = document.createElement("span");
      p.className = "sync-estado";
      p.textContent = "✓ progreso sincronizado";
      const b = document.createElement("button");
      b.className = "sync-salir";
      b.type = "button";
      b.textContent = "Salir";
      b.addEventListener("click", salir);
      contenedor.append(p, b);
    } else {
      const d = document.createElement("div");
      d.id = "g-boton";
      contenedor.append(d);
      if (window.google?.accounts?.id) {
        google.accounts.id.initialize({
          client_id: clientIdGuardado,
          callback: (resp) => entrar(resp.credential)
        });
        google.accounts.id.renderButton(d, { theme: "outline", size: "medium", text: "signin", locale: "es" });
      }
    }
  }

  function montar(sel, clientId) {
    contenedor = document.querySelector(sel);
    clientIdGuardado = clientId;
    const guardada = sessionStorage.getItem(CLAVE_CRED);
    if (guardada) {
      // Se reusa la credencial de la pestaña; si venció, el 401 la limpia sola.
      cred = guardada;
      pedir("GET")
        .then(remoto => {
          if (remoto) {
            Progreso.guardar(Progreso.fusionar(Progreso.leer(), remoto));
            dispatchEvent(new CustomEvent("progreso-actualizado"));
          }
        })
        .catch(() => {});
    }
    pintar();
  }

  return { montar, encolar, entrar, salir, sesionActiva };
})();

if (typeof module !== "undefined") module.exports.Sync = Sync;
```

- [ ] **Paso 3: Estilos**

```css
.sync { display: flex; align-items: center; gap: var(--e3); }
.sync-estado { font-family: var(--font-mono); font-size: 0.75rem; color: var(--accent); }
.sync-salir {
  font: inherit; font-size: 0.75rem; cursor: pointer; color: var(--ink-mute);
  background: none; border: 1px solid var(--rule); border-radius: 999px;
  padding: 2px var(--e3);
}
```

- [ ] **Paso 4: Verificar el orden de carga**

`progreso.js` referencia `Sync` dentro de `Progreso.guardar` con `typeof Sync !== "undefined"`. Como `Sync` se declara con `const` en el mismo archivo, después de `Progreso`, la referencia se resuelve recién en tiempo de ejecución, cuando ya está inicializado. Confirmar en el navegador que `Progreso.registrar("x", "y", 100)` desde la consola no tira `ReferenceError`.

- [ ] **Paso 5: Commit**

```bash
git add progreso.js shared.css .env.example
git commit -m "feat: login con Google y sincronización de progreso con debounce"
```

---

### Tarea 16: `index.html` — hero y roadmap

**Files:**
- Create: `index.html`
- Create: `index.js`

**Interfaces:**
- Consumes: `data/temario.json`, `Progreso.estadoTema`, `Progreso.porcentaje`, `Sync.montar`.
- Produces: nada que consuman otras tareas.

- [ ] **Paso 1: Escribir el HTML**

Home liviano, como manda el skill: hero, roadmap, tres accesos, cierre. El listado completo de temas **no** va acá, va en `temas.html`.

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Machine Learning · Estudio</title>
  <meta name="description" content="Machine learning desde cero: teoría, diagramas animados y ejercicios interactivos.">
  <link rel="stylesheet" href="shared.css">
  <script src="shared.js"></script>
</head>
<body>
  <nav>
    <a href="index.html" class="nav-brand">ML · Estudio</a>
    <div class="nav-right">
      <a href="index.html" class="nav-link on">Inicio</a>
      <a href="temas.html" class="nav-link">Temas</a>
      <!-- Estudio y Práctica se agregan en la Etapa 6. -->
      <div class="sync" id="sync"></div>
      <button class="theme-tog" id="theme-tog" onclick="toggleTheme()" aria-label="Cambiar tema">☾</button>
    </div>
  </nav>

  <main class="envoltura">
    <header class="hero">
      <p class="eyebrow">De cero a redes neuronales</p>
      <h1><span class="w1">Machine</span><span class="w2">Learning</span></h1>
      <p class="lede">Veinticuatro temas, seis fases. Empieza en "qué es una variable" y termina en una red que aprende sola. Cada concepto se ve moverse y se practica antes de pasar al siguiente.</p>
      <p class="progreso-global">Progreso: <strong id="pct">0</strong>% · <span id="completos">0</span> de 24 temas</p>
    </header>

    <section id="roadmap" aria-label="Roadmap de aprendizaje"></section>

    <section class="section reveal">
      <div class="sec-no">→</div>
      <div class="sec-body">
        <h2>Cómo estudiar esto</h2>
        <p>Los temas están en orden y el orden importa…</p>
      </div>
    </section>
  </main>

  <footer class="envoltura pie">
    <p>Hecho para David y Abel · <span data-anio></span></p>
  </footer>

  <script src="https://accounts.google.com/gsi/client" async defer></script>
  <script src="progreso.js"></script>
  <script src="index.js"></script>
</body>
</html>
```

- [ ] **Paso 2: Escribir `index.js`**

```js
const CLIENT_ID = "REEMPLAZAR_CON_EL_CLIENT_ID.apps.googleusercontent.com";

/* Cuántos ejercicios corregibles tiene cada tema. Se completa a medida
   que se escriben los temas; un tema ausente cuenta como 0. */
const EJERCICIOS_POR_TEMA = {
  "regresion-lineal": 5
};

async function pintarRoadmap() {
  const temario = await fetch("data/temario.json").then(r => r.json());
  const estado = Progreso.leer();
  const cont = document.getElementById("roadmap");
  cont.replaceChildren();

  for (const fase of temario.fases) {
    const bloque = document.createElement("div");
    bloque.className = `rm-phase fase-${fase.n}`;

    const meta = document.createElement("div");
    meta.className = "rm-phase-meta";
    meta.innerHTML = `
      <div class="rm-phase-n">${String(fase.n).padStart(2, "0")}</div>
      <h2>${fase.nombre}</h2>
      <p class="rm-phase-hint">${fase.hint}</p>`;

    const grilla = document.createElement("div");
    grilla.className = "rm-cards";

    for (const tema of fase.temas) {
      const total = EJERCICIOS_POR_TEMA[tema.slug] || 0;
      const est = Progreso.estadoTema(estado, tema.slug, total);
      const existe = Boolean(EJERCICIOS_POR_TEMA[tema.slug]);

      const a = document.createElement("a");
      a.className = `rm-card ${est === "completa" ? "completa" : est === "en-progreso" ? "en-progreso" : ""}`;
      a.href = tema.archivo;
      if (!existe) { a.classList.add("proximamente"); a.removeAttribute("href"); }
      a.innerHTML = `
        <div class="rm-card-n">${String(tema.n).padStart(2, "0")}</div>
        <div class="rm-card-t">${tema.titulo}</div>
        <span class="rm-card-estado">${
          !existe ? "próximamente" :
          est === "completa" ? "✓ completo" :
          est === "en-progreso" ? "en progreso" : "sin empezar"
        }</span>`;
      grilla.append(a);
    }

    bloque.append(meta, grilla);
    cont.append(bloque);
  }

  const pct = Progreso.porcentaje(estado, temario, EJERCICIOS_POR_TEMA);
  document.getElementById("pct").textContent = pct;
  document.getElementById("completos").textContent =
    temario.fases.flatMap(f => f.temas)
      .filter(t => Progreso.estadoTema(estado, t.slug, EJERCICIOS_POR_TEMA[t.slug] || 0) === "completa").length;
}

addEventListener("DOMContentLoaded", () => {
  pintarRoadmap();
  Sync.montar("#sync", CLIENT_ID);
});
addEventListener("progreso-actualizado", pintarRoadmap);
```

- [ ] **Paso 3: Estilos del pie y de las tarjetas "próximamente"**

```css
.pie { padding-block: var(--e16); border-top: 1px solid var(--rule); font-size: var(--t-small); color: var(--ink-mute); }
.progreso-global { font-family: var(--font-mono); font-size: var(--t-small); color: var(--ink-mute); }
.progreso-global strong { color: var(--accent); }
.rm-card.proximamente { opacity: 0.45; cursor: default; pointer-events: none; }
```

- [ ] **Paso 4: Probar**

Servir la carpeta con `npx serve` o `python -m http.server` — abrir con `file://` rompe el `fetch` de `data/temario.json` por CORS.

Confirmar:
- Las seis fases se pintan con su color propio.
- Solo la tarjeta de regresión lineal es clickeable; las otras 23 salen atenuadas.
- Resolver un ejercicio en el tema y volver al home actualiza el estado de la tarjeta.

- [ ] **Paso 5: Commit**

```bash
git add index.html index.js shared.css
git commit -m "feat: home con roadmap de 6 fases pintado desde el temario"
```

---

### Tarea 17: `temas.html` — índice completo

**Files:**
- Create: `temas.html`
- Create: `temas.js`

**Interfaces:**
- Consumes: `data/temario.json`.
- Produces: nada que consuman otras tareas.

- [ ] **Paso 1: Escribir el marco de la página**

Mismo `head` y `nav` que `index.html`, con `class="nav-link on"` sobre el link a `temas.html` y no sobre el de `index.html`. Cuatro secciones numeradas:

```html
<main class="envoltura">
  <header class="hero">
    <p class="eyebrow">Índice completo</p>
    <h1><span class="w1">Temas</span><span class="w2">y territorio</span></h1>
    <p class="lede">El mapa entero de la materia: de dónde viene, cómo se divide, y qué distinciones son las que más se confunden.</p>
  </header>

  <section class="section reveal fase-1">
    <div class="sec-no">01</div>
    <div class="sec-body">
      <h2>Las tres ramas</h2>
      <div class="pillars">
        <article class="pilar">
          <h3>Supervisado</h3>
          <p class="pilar-e"><strong>Entra:</strong> ejemplos con la respuesta puesta.</p>
          <p class="pilar-s"><strong>Sale:</strong> una función que predice la respuesta de un ejemplo nuevo.</p>
          <p>Filtrar spam: miles de mails ya marcados como spam o no.</p>
        </article>
        <article class="pilar">
          <h3>No supervisado</h3>
          <p class="pilar-e"><strong>Entra:</strong> ejemplos sin ninguna respuesta.</p>
          <p class="pilar-s"><strong>Sale:</strong> estructura que estaba escondida en los datos.</p>
          <p>Agrupar clientes por comportamiento sin saber de antemano qué grupos hay.</p>
        </article>
        <article class="pilar">
          <h3>Por refuerzo</h3>
          <p class="pilar-e"><strong>Entra:</strong> un entorno que premia y castiga.</p>
          <p class="pilar-s"><strong>Sale:</strong> una política de qué hacer en cada situación.</p>
          <p>Un programa que aprende a jugar al ajedrez jugando millones de partidas.</p>
        </article>
      </div>
    </div>
  </section>

  <section class="section reveal fase-3">
    <div class="sec-no">02</div>
    <div class="sec-body">
      <h2>Doscientos años en siete hitos</h2>
      <ol class="timeline" id="timeline"></ol>
    </div>
  </section>

  <section class="section reveal fase-2">
    <div class="sec-no">03</div>
    <div class="sec-body">
      <h2>Los 24 temas</h2>
      <div id="lista-temas"></div>
    </div>
  </section>

  <section class="section reveal fase-4">
    <div class="sec-no">04</div>
    <div class="sec-body">
      <h2>Distinciones que se confunden</h2>
      <div class="dist-grid" id="distinciones"></div>
    </div>
  </section>
</main>
```

- [ ] **Paso 2: Escribir `temas.js`**

```js
const HITOS = [
  { anio: "1805", t: "Mínimos cuadrados", d: "Legendre publica el método que todavía hoy es la base de la regresión lineal." },
  { anio: "1957", t: "Perceptrón", d: "Rosenblatt construye la primera neurona artificial que aprende de sus errores." },
  { anio: "1986", t: "Backpropagation", d: "Rumelhart, Hinton y Williams lo popularizan: por fin se pueden entrenar redes de varias capas." },
  { anio: "1995", t: "Support Vector Machines", d: "Cortes y Vapnik. Dominan la clasificación durante quince años." },
  { anio: "2001", t: "Random Forest", d: "Breiman: muchos árboles mediocres votando le ganan a un árbol muy bueno." },
  { anio: "2012", t: "AlexNet", d: "Una red convolucional gana ImageNet por paliza y arranca la era del deep learning." },
  { anio: "2017", t: "Transformers", d: "«Attention is all you need». Es la arquitectura detrás de todo lo que vino después." }
];

const DISTINCIONES = [
  { a: "Regresión", b: "Clasificación",
    trampa: "Las dos predicen, pero una devuelve un número continuo y la otra una categoría.",
    truco: "¿La respuesta admite un punto y medio? Si sí, es regresión." },
  { a: "Overfitting", b: "Underfitting",
    trampa: "Overfitting se memorizó los datos de entrenamiento; underfitting ni siquiera los aprendió.",
    truco: "Mirá los dos errores: si el de train es bajo y el de test alto, es overfitting. Si los dos son altos, underfitting." },
  { a: "Paramétrico", b: "No paramétrico",
    trampa: "No paramétrico no significa «sin parámetros», sino que la cantidad crece con los datos.",
    truco: "Regresión lineal siempre tiene los mismos coeficientes. KNN guarda el dataset entero." },
  { a: "Bagging", b: "Boosting",
    trampa: "Los dos combinan modelos débiles, pero bagging los entrena en paralelo y boosting en cadena.",
    truco: "Bagging vota en asamblea. Boosting es una fila donde cada uno corrige al anterior." },
  { a: "Supervisado", b: "No supervisado",
    trampa: "La diferencia no es el algoritmo, es si los datos vienen con la respuesta.",
    truco: "¿Hay una columna «y»? Supervisado." }
];

function pintarTimeline() {
  const ol = document.getElementById("timeline");
  for (const h of HITOS) {
    const li = document.createElement("li");
    li.className = "tl-item";
    li.innerHTML = `<span class="tl-anio">${h.anio}</span>
                    <div><h3>${h.t}</h3><p>${h.d}</p></div>`;
    ol.append(li);
  }
}

function pintarDistinciones() {
  const cont = document.getElementById("distinciones");
  for (const d of DISTINCIONES) {
    const art = document.createElement("article");
    art.className = "dist";
    art.innerHTML = `<h3>${d.a} <span class="dist-vs">vs</span> ${d.b}</h3>
                     <p class="dist-trampa">${d.trampa}</p>
                     <p class="dist-truco"><strong>Truco:</strong> ${d.truco}</p>`;
    cont.append(art);
  }
}

async function pintarListaTemas() {
  const temario = await fetch("data/temario.json").then(r => r.json());
  const cont = document.getElementById("lista-temas");
  for (const fase of temario.fases) {
    const bloque = document.createElement("div");
    bloque.className = `fase-bloque fase-${fase.n}`;
    const ol = document.createElement("ol");
    ol.className = "concept-list";
    for (const tema of fase.temas) {
      const li = document.createElement("li");
      li.className = "ci";
      const existe = EXISTENTES.has(tema.slug);
      li.innerHTML = `<span class="ci-n">${String(tema.n).padStart(2, "0")}</span>
                      <span class="ci-t">${tema.titulo}</span>
                      ${existe ? `<a class="ci-ver" href="${tema.archivo}">Ver concepto →</a>`
                               : `<span class="ci-ver ci-pronto">próximamente</span>`}`;
      ol.append(li);
    }
    bloque.innerHTML = `<h3 class="fase-tit">Fase ${fase.n} · ${fase.nombre}</h3>`;
    bloque.append(ol);
    cont.append(bloque);
  }
}

/* Temas ya escritos. Se agrega un slug acá cuando su página existe. */
const EXISTENTES = new Set(["regresion-lineal"]);

addEventListener("DOMContentLoaded", () => {
  pintarTimeline();
  pintarDistinciones();
  pintarListaTemas();
});
```

- [ ] **Paso 3: Estilos en `shared.css`**

```css
/* ── Pilares ─────────────────────────────────────────────── */
.pillars { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: var(--e4); }
.pilar { padding: var(--e6); background: var(--panel); border: 1px solid var(--rule); border-radius: var(--radio); }
.pilar h3 { color: var(--accent); }
.pilar-e, .pilar-s { font-size: var(--t-small); color: var(--ink-mute); margin-bottom: var(--e2); }

/* ── Línea de tiempo ─────────────────────────────────────── */
.timeline { list-style: none; margin: 0; padding: 0 0 0 var(--e8); border-left: 1px solid var(--rule); }
.tl-item { position: relative; display: grid; grid-template-columns: 5rem 1fr; gap: var(--e4); padding-block: var(--e6); }
.tl-item::before {
  content: ""; position: absolute; left: calc(var(--e8) * -1 - 4px); top: var(--e8);
  width: 7px; height: 7px; border-radius: 50%; background: var(--accent);
}
.tl-anio { font-family: var(--font-mono); font-weight: 700; color: var(--accent); }
.tl-item h3 { margin-bottom: var(--e2); }
.tl-item p { font-size: var(--t-small); color: var(--ink-mute); margin: 0; }

/* ── Lista de conceptos ──────────────────────────────────── */
.fase-bloque { margin-bottom: var(--e12); }
.fase-tit { font-size: var(--t-h3); color: var(--accent); margin-bottom: var(--e4); }
.concept-list { list-style: none; margin: 0; padding: 0; }
.ci {
  display: grid; grid-template-columns: 3rem 1fr auto; align-items: baseline; gap: var(--e4);
  padding-block: var(--e3); border-bottom: 1px solid var(--rule);
}
.ci-n { font-family: var(--font-mono); font-size: var(--t-small); color: var(--ink-mute); }
.ci-t { font-weight: 500; }
.ci-ver { font-size: var(--t-small); opacity: 0; transition: opacity var(--transicion); }
.ci:hover .ci-ver, .ci-ver:focus-visible { opacity: 1; }
.ci-pronto { opacity: 0.4; color: var(--ink-mute); }

/* ── Distinciones ────────────────────────────────────────── */
.dist-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: var(--e4); }
.dist { padding: var(--e6); background: var(--panel); border: 1px solid var(--rule); border-radius: var(--radio); }
.dist-vs { font-family: var(--font-mono); font-weight: 400; color: var(--accent); font-size: 0.8em; }
.dist-trampa { font-size: var(--t-small); }
.dist-truco { font-size: var(--t-small); color: var(--ink-mute); margin: 0; }
```

Nota: `.ci-ver` se revela con hover **y con `:focus-visible`**. Sin la segunda regla, el enlace sería inalcanzable navegando con teclado.

- [ ] **Paso 4: Verificar**

Run: `node verificar.js`
Expected: OK.

En el navegador: los 24 temas listados, los enlaces de los no escritos atenuados igual que en el home, y `nav-link.on` sobre "Temas". Recorrer la lista de conceptos solo con Tab y confirmar que los enlaces "Ver concepto →" aparecen al recibir foco.

- [ ] **Paso 5: Commit**

```bash
git add temas.html temas.js shared.css
git commit -m "feat: página de temas con ramas, línea de tiempo, índice y distinciones"
```

---

### Tarea 18: Completar `verificar.js`

**Files:**
- Modify: `verificar.js`
- Modify: `verificar.test.js`

**Interfaces:**
- Consumes: todos los HTML ya escritos.
- Produces: `verificar()` amplíado con los chequeos de navegación, enlaces e ids duplicados.

- [ ] **Paso 1: Escribir los tests que fallan**

Agregar a `verificar.test.js`:

```js
const { _chequearNav, _chequearIds } = require("./verificar.js");

test("_chequearNav detecta la marca 'on' en el link equivocado", () => {
  const html = `<a href="index.html" class="nav-link on">Inicio</a>
                <a href="temas.html" class="nav-link">Temas</a>`;
  assert.strictEqual(_chequearNav(html, "temas.html").length, 1);
  assert.strictEqual(_chequearNav(html, "index.html").length, 0);
});

test("_chequearNav detecta la ausencia total de nav", () => {
  assert.ok(_chequearNav("<p>nada</p>", "index.html").length > 0);
});

test("_chequearIds detecta ids de ejercicio duplicados", () => {
  const js = `Ejercicios.montar("#e", [{tipo:"mcq", id:"a1"},{tipo:"num", id:"a1"}], "t");`;
  assert.strictEqual(_chequearIds(js).length, 1);
});

test("_chequearIds no marca ids distintos", () => {
  const js = `Ejercicios.montar("#e", [{tipo:"mcq", id:"a1"},{tipo:"num", id:"a2"}], "t");`;
  assert.strictEqual(_chequearIds(js).length, 0);
});
```

- [ ] **Paso 2: Correr y verificar que falla**

Run: `node --test verificar.test.js`
Expected: FAIL — `_chequearNav is not a function`

- [ ] **Paso 3: Implementar**

```js
function _chequearNav(html, archivo) {
  const errores = [];
  const links = [...html.matchAll(/<a\s+href="([^"]+)"\s+class="nav-link([^"]*)"/g)];
  if (!links.length) return [`${archivo}: no tiene bloque de navegación`];
  for (const [, href, extra] of links) {
    const marcado = extra.includes("on");
    if (marcado && href !== archivo) errores.push(`${archivo}: marca 'on' en el link a ${href}`);
    if (!marcado && href === archivo) errores.push(`${archivo}: le falta la marca 'on' en su propio link`);
  }
  return errores;
}

function _chequearIds(js) {
  const ids = [...js.matchAll(/\bid:\s*["']([^"']+)["']/g)].map(m => m[1]);
  const vistos = new Set();
  const dup = new Set();
  for (const id of ids) { if (vistos.has(id)) dup.add(id); vistos.add(id); }
  return [...dup].map(id => `id de ejercicio duplicado: ${id}`);
}

function _chequearEnlaces(html, archivo, existentes, declarados) {
  const errores = [];
  for (const [, href] of html.matchAll(/href="([^"#?:]+\.html)[^"]*"/g)) {
    if (existentes.has(href)) continue;
    // Un tema declarado en temario.json pero todavía sin escribir no es un
    // enlace roto: es un tema pendiente. Se distingue para que el verificador
    // sirva durante toda la Etapa 5, cuando faltan 23 archivos por escribir.
    if (declarados.has(href)) continue;
    errores.push(`${archivo}: enlace roto a ${href}`);
  }
  return errores;
}
```

Y en `verificar()`, pasarle los dos conjuntos: `existentes` son los `.html` que están en disco, `declarados` son los `archivo` de `data/temario.json`.

Nota: que un tema pendiente no cuente como enlace roto es lo que permite que
`node verificar.js` siga siendo útil durante la Etapa 5. El contrapeso es que
las páginas nunca deben *mostrar* un enlace clickeable a un tema sin escribir —
de eso se ocupan `.rm-card.proximamente` en el home y `.ci-pronto` en temas.

Cablearlos dentro de `verificar()`: recorrer todos los `.html` de la raíz (salvo `muestra.html`), aplicar `_chequearNav` y `_chequearEnlaces` a cada uno, y `_chequearIds` a cada `.js`. Exportar los tres helpers junto con `verificar`.

- [ ] **Paso 4: Correr todo**

Run: `node --test`
Expected: PASS.

Run: `node verificar.js`
Expected: `✓ verificación OK`. Si sale algún error, es un error real del sitio — arreglarlo, no aflojar el chequeo.

- [ ] **Paso 5: Commit**

```bash
git add verificar.js verificar.test.js
git commit -m "feat: chequeos de navegación, enlaces e ids duplicados"
```

---

### Tarea 19: Deploy a Vercel

**Files:**
- Create: `vercel.json`
- Create: `README.md`

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: el sitio en vivo.

- [ ] **Paso 1: Escribir `vercel.json`**

```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "cleanUrls": true,
  "functions": { "api/*.js": { "runtime": "nodejs20.x" } },
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
```

Sin `buildCommand` ni `outputDirectory`: no hay build, Vercel sirve la raíz.

- [ ] **Paso 2: Crear el ID de cliente de Google**

En Google Cloud Console:
1. Crear un proyecto.
2. APIs y servicios → Pantalla de consentimiento de OAuth → Externo → completar nombre y correo de soporte.
3. Credenciales → Crear credenciales → ID de cliente de OAuth → Aplicación web.
4. Orígenes de JavaScript autorizados: `http://localhost:3000` y la URL de producción de Vercel.
5. Copiar el ID de cliente.

Pegarlo en la constante `CLIENT_ID` de `index.js` y en la variable de entorno de Vercel.

- [ ] **Paso 3: Provisionar Redis**

En el panel de Vercel: Storage → Marketplace → Upstash for Redis → crear una base en el plan gratuito y conectarla al proyecto. Eso inyecta `KV_REST_API_URL` y `KV_REST_API_TOKEN` solo.

Verificar el nombre exacto de las variables que quedan inyectadas; si difieren de las esperadas, `configRedis()` ya contempla los dos juegos de nombres, pero conviene confirmarlo.

- [ ] **Paso 4: Cargar las variables de entorno**

En Vercel → Settings → Environment Variables, agregar para Production y Preview:
- `GOOGLE_CLIENT_ID`
- `EMAILS_PERMITIDOS` — los tres correos separados por coma

- [ ] **Paso 5: Desplegar**

```bash
npx vercel --prod
```

- [ ] **Paso 6: Verificar en producción**

1. Abrir la URL. El home carga, el roadmap se pinta.
2. Entrar al tema de regresión lineal, resolver un ejercicio sin loguearse. El progreso aparece.
3. Loguearse con Google. Confirmar que el progreso hecho sin sesión **no se perdió** — es lo que hace la fusión.
4. Abrir la misma URL en el teléfono, loguearse con la misma cuenta, confirmar que el progreso está.
5. Probar con una cuenta de Google fuera de la allowlist: debe fallar el guardado, pero el sitio tiene que seguir funcionando contra `localStorage`.
6. Con las DevTools: `curl -X POST <url>/api/progress -d '{}'` sin header de autorización debe devolver 401.

- [ ] **Paso 7: Escribir `README.md`**

Qué es el proyecto, cómo correrlo local (`npx serve`), cómo correr los tests (`node --test`), cómo correr el verificador (`node verificar.js`), qué variables de entorno hacen falta, y cómo agregar un tema nuevo (editar `data/temario.json`, copiar la estructura de `concept-09`, sumar la cuenta a `EJERCICIOS_POR_TEMA` en `index.js`).

- [ ] **Paso 8: Commit**

```bash
git add vercel.json README.md index.js
git commit -m "feat: configuración de deploy en Vercel y documentación del proyecto"
```

---

## Qué queda fuera de este plan

- Los 23 temas restantes (Etapa 5). Plan propio, después del checkpoint de la Tarea 12.
- `estudio.html` y `practica.html` (Etapa 6). Agregan contenido que todavía no existe.
- El nombre y la marca definitiva del sitio. Decidirlo antes de la Tarea 16, donde se escribe la nav.
