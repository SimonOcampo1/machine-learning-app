{
  "name": "ml-estudio",
  "private": true,
  "type": "commonjs",
  "engines": { "node": ">=20" },
  "scripts": {
    "verificar": "node scripts/verificar.js",
    "contraste": "node scripts/contraste.js",
    "test": "node --test",
    "check": "npm run verificar && npm run contraste && npm test"
  }
}
